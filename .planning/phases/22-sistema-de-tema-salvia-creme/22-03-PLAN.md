---
phase: 22-sistema-de-tema-salvia-creme
plan: 03
type: execute
wave: 3
depends_on: [22-02]
files_modified:
  - src/App.tsx
autonomous: false
requirements: [VIS-03]
must_haves:
  truths:
    - "O cuidador ve um seletor de 3 temas na configuracao, no lugar do toggle de alto contraste"
    - "Tocar uma opcao de tema troca o tema do app imediatamente"
    - "O tema escolhido permanece apos reiniciar o app"
  artifacts:
    - path: "src/App.tsx"
      provides: "Seletor de 3 temas na secao de acessibilidade da config existente"
      contains: "setThemeName"
  key_links:
    - from: "seletor de tema na config"
      to: "estado themeName"
      via: "onPress chama setThemeName"
      pattern: "setThemeName\\("
---

<objective>
Adicionar o seletor de 3 temas ("Salvia & Creme", "Terracota", "Sereno Escuro") a tela de
configuracao EXISTENTE, na secao de acessibilidade, substituindo o toggle binario de alto
contraste. Inclui um checkpoint de verificacao humana da troca de tema em device/emulador.

Purpose: entregar VIS-03 — o cuidador escolhe entre os 3 temas e o app reflete a troca
imediatamente e a mantem apos reiniciar.

Output: `src/App.tsx` com o seletor de tema na config + verificacao da fase.

Escopo: SOMENTE o controle de selecao de tema dentro da secao `acessibilidade` da config
atual. Este plano NAO redesenha a tela de configuracao (drill-down e fase 24). Reusa o
componente `OptionChip` existente — mesmo padrao visual das outras opcoes da config.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-sistema-de-tema-salvia-creme/22-CONTEXT.md
@.planning/phases/22-sistema-de-tema-salvia-creme/22-02-SUMMARY.md
@src/theme.ts
@src/App.tsx

<interfaces>
<!-- Apos os planos 01 e 02, src/App.tsx ja tem: -->
<!--   const [themeName, setThemeName] = useState<ThemeName>('default'); -->
<!--   const theme = useMemo(() => resolveTheme(themeName), [themeName]); -->
<!--   const isDarkTheme = theme.isDark; -->
<!--   const isHighContrast = isDarkTheme;  <- alias LOCKED pelo plano 02 (sempre presente) -->
<!--   ThemeName = 'default' | 'terracota' | 'sereno-escuro' -->
<!--   persistencia automatica de themeName via useEffect. -->

<!-- Secao de config a editar — src/App.tsx ~linha 3532-3557: -->
<!--   {configSection === 'acessibilidade' && ( -->
<!--     <View style={[styles.configSectionCard, ...]}> -->
<!--       <Text ...>Acessibilidade</Text> -->
<!--       <View style={styles.iosToggleRow}>  <- toggle "Alto contraste" (Switch) a REMOVER -->
<!--       <View style={styles.iosToggleRow}>  <- toggle "Feedback visual" (Switch) a MANTER -->
<!--   ... -->

<!-- Componente OptionChip existente — src/App.tsx ~linha 3789: -->
<!--   function OptionChip({ label, active, onPress, highContrast }: { -->
<!--     label: string; active: boolean; onPress: () => void; highContrast?: boolean }) -->
<!--   Ja usado para escala de UI, grid columns, velocidade/tom de voz. -->

<!-- styles.settingActions e styles.settingLabel ja existem (usados na secao 'perfil'). -->
</interfaces>

<from_summary>
Ler `.planning/phases/22-sistema-de-tema-salvia-creme/22-02-SUMMARY.md` para confirmar o
nome final do estado/setter de tema (`themeName` / `setThemeName`). O alias `isHighContrast`
foi LOCKED como obrigatorio no plano 02 (`const isHighContrast = isDarkTheme;`) — este plano
usa `isHighContrast` diretamente, sem condicional. Usar os nomes reais reportados no SUMMARY
caso `themeName`/`setThemeName` divirjam.
</from_summary>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Substituir o toggle de alto contraste pelo seletor de 3 temas</name>
  <read_first>
    - src/App.tsx (linhas 3532-3557: bloco configSection === 'acessibilidade')
    - src/App.tsx (linhas 3506-3530: secao 'perfil' como referencia de padrao OptionChip + settingLabel + settingActions)
    - src/App.tsx (linhas 3789-3815: componente OptionChip)
    - src/theme.ts (ThemeName e os 3 temas para os rotulos)
    - .planning/phases/22-sistema-de-tema-salvia-creme/22-02-SUMMARY.md (nomes finais do estado de tema)
  </read_first>
  <files>src/App.tsx</files>
  <action>
    No bloco `configSection === 'acessibilidade'` (~linha 3532-3557) de `src/App.tsx`:

    1. REMOVER o primeiro `<View style={styles.iosToggleRow}>` — o que contem o `<Text>`
       "Alto contraste" e o `<Switch>` ligado a `contrastMode`/`setContrastMode`
       (linhas ~3535-3544). Esse toggle nao existe mais; o estado contrastMode foi removido
       no plano 02.

    2. MANTER intacto o segundo `<View style={styles.iosToggleRow}>` — o toggle
       "Feedback visual" (`visualFeedbackEnabled` / `setVisualFeedbackEnabled`).

    3. ADICIONAR, antes do toggle "Feedback visual", o seletor de tema, seguindo o padrao
       visual da secao 'perfil' (label + linha de OptionChips). O alias `isHighContrast`
       existe garantidamente (locked no plano 02) — usar `isHighContrast` direto:
       ```
       <Text style={[styles.settingLabel, isHighContrast && styles.textHighContrast]}>Tema do aplicativo</Text>
       <View style={styles.settingActions}>
         <OptionChip
           label="Salvia & Creme"
           active={themeName === 'default'}
           highContrast={isHighContrast}
           onPress={() => setThemeName('default')}
         />
         <OptionChip
           label="Terracota"
           active={themeName === 'terracota'}
           highContrast={isHighContrast}
           onPress={() => setThemeName('terracota')}
         />
         <OptionChip
           label="Sereno Escuro"
           active={themeName === 'sereno-escuro'}
           highContrast={isHighContrast}
           onPress={() => setThemeName('sereno-escuro')}
         />
       </View>
       ```

    4. Atualizar o texto de dica (`styles.modalHint`) ao final da secao para refletir o tema
       em vez do "modo reduzido"/contraste. Texto pt-BR sugerido:
       "O tema muda as cores do aplicativo. Sereno Escuro e um modo escuro calmo para uso
       noturno ou criancas sensiveis a brilho."

    5. Garantir que nenhuma referencia remanescente a `contrastMode` / `setContrastMode`
       sobrou no arquivo (o plano 02 ja removeu o estado; esta task remove o ultimo uso
       na UI).

    NAO alterar a estrutura de abas/secoes da config (drill-down e fase 24). Apenas trocar
    o conteudo interno da secao 'acessibilidade'.
  </action>
  <verify>
    <automated>npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "contrastMode" src/App.tsx` retorna 0.
    - `grep "setThemeName('default')" src/App.tsx` encontra a opcao Salvia & Creme.
    - `grep "setThemeName('terracota')" src/App.tsx` encontra a opcao Terracota.
    - `grep "setThemeName('sereno-escuro')" src/App.tsx` encontra a opcao Sereno Escuro.
    - `grep "Tema do aplicativo" src/App.tsx` encontra o label do seletor.
    - `grep "Alto contraste" src/App.tsx` retorna 0 ocorrencias (toggle removido).
    - `npm run lint` (`tsc --noEmit`) passa sem erros.
    - `npm run test` sem novas falhas alem das 4 pre-existentes herdadas.
  </acceptance_criteria>
  <done>
    A secao de acessibilidade mostra um seletor de 3 temas via OptionChip; o toggle de alto
    contraste foi removido; lint e testes passam.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verificacao humana da troca de tema em device/emulador</name>
  <what-built>
    Fase 22 completa: src/theme.ts com 3 temas Salvia & Creme + fonte Nunito; src/App.tsx
    com estado themeName, factory makeStyles(theme), fonte Nunito carregada no boot, shell
    aplicando o tema e o seletor de 3 temas na secao de acessibilidade da config.
  </what-built>
  <how-to-verify>
    1. Rodar o app: `npm run start` e abrir no emulador/device (iOS preferencial, conforme
       prioridade do projeto).
    2. Observar a tela inicial: o fundo deve ser creme quente (`#F1EBDD`), NAO branco/cinza
       iOS; os textos devem aparecer na fonte Nunito (arredondada), nao na fonte de sistema.
    3. Abrir a configuracao do cuidador e ir na secao "Acessibilidade".
       - Confirmar que existe o seletor "Tema do aplicativo" com 3 opcoes
         (Salvia & Creme / Terracota / Sereno Escuro) e que o toggle "Alto contraste"
         NAO existe mais.
    4. Tocar "Terracota": o fundo do app deve mudar imediatamente para o creme mais quente
       do tema terracota.
    5. Tocar "Sereno Escuro": o app deve ficar no modo escuro calmo (fundo marrom-carvao
       `#211F1B`, texto claro), com a status bar em conteudo claro.
    6. Tocar "Salvia & Creme": volta ao tema padrao.
    7. Com "Sereno Escuro" selecionado, FECHAR e REABRIR o app completamente.
       - Confirmar que o app reabre ja no tema Sereno Escuro (persistencia OK).
    8. Confirmar que nenhum elemento exibe o azul iOS #007AFF como cor de acento.
  </how-to-verify>
  <acceptance_criteria>
    - Os 3 temas trocam o visual do app imediatamente ao serem tocados.
    - O tema escolhido persiste apos reiniciar o app.
    - Os textos usam a fonte Nunito.
    - Nenhum azul iOS #007AFF visivel como cor primaria.
  </acceptance_criteria>
  <resume-signal>Digite "aprovado" ou descreva os problemas visuais encontrados.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (nenhuma) | Fase exclusivamente visual — sem novas fronteiras de confianca |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-03 | (n/a) | src/App.tsx (seletor de tema na config) | accept | Sem superficie de ataque nova — fase exclusivamente visual (tokens, fonte, tema); nenhuma mudanca em auth, rede, persistencia de dados sensiveis ou entrada de usuario. O seletor apenas troca um enum de tema ja persistido pelo plano 02. |
</threat_model>

<verification>
- `npm run lint` sem erros.
- `npm run test` sem novas falhas alem das 4 pre-existentes herdadas.
- Verificacao humana: 3 temas trocam ao vivo, persistem apos reiniciar, fonte Nunito visivel,
  nenhum azul iOS.
</verification>

<success_criteria>
- Seletor de 3 temas presente na secao de acessibilidade; toggle de alto contraste removido.
- Troca de tema reflete imediatamente no app.
- Tema escolhido persiste apos reiniciar.
- VIS-03 satisfeito e validado em device/emulador.
</success_criteria>

<output>
After completion, create `.planning/phases/22-sistema-de-tema-salvia-creme/22-03-SUMMARY.md`
</output>
</content>
