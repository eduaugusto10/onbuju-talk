---
phase: 22-sistema-de-tema-salvia-creme
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/theme.ts
  - package.json
  - package-lock.json
autonomous: true
requirements: [VIS-01, VIS-02]
must_haves:
  truths:
    - "src/theme.ts exporta 3 temas (default, terracota, sereno-escuro) com a paleta Salvia & Creme"
    - "Nenhum tema da nova API expoe o azul iOS #007AFF como cor primaria"
    - "A familia de fonte Nunito esta disponivel como token tipografico"
    - "Os exports legacy iOS continuam intactos verbatim para src/ui/* compilar"
  artifacts:
    - path: "src/theme.ts"
      provides: "Tipo Theme, registro themes, funcao resolveTheme, tokens dos 3 temas, tokens de fonte Nunito, exports legacy iOS verbatim"
      contains: "export const themes"
    - path: "package.json"
      provides: "Dependencias @expo-google-fonts/nunito e expo-font"
      contains: "@expo-google-fonts/nunito"
  key_links:
    - from: "src/theme.ts"
      to: "sources/themes/default.css"
      via: "valores de token portados literalmente"
      pattern: "#6F9D86"
---

<objective>
Estender `src/theme.ts` com o sistema de tema "Salvia & Creme": adicionar 3 objetos de tema
completos (default / terracota / sereno-escuro) com a paleta terrosa de baixo estimulo, tokens
de fonte Nunito, raios generosos e sombras suaves tingidas de quente. Os exports iOS atuais
(`colors`, `typography`, `radii`, `spacing`, `shadows`, `palette`, `tiles`, `CHILD_GRID_COLUMNS`)
sao PRESERVADOS verbatim, lado a lado com a nova API. Tambem instala as dependencias da fonte
Nunito.

Purpose: estabelecer a fundacao de tokens que as fases 23 e 24 (e a integracao da fase 22 no
shell, plano 02) consomem. Sem essa fundacao nenhuma tela pode ser reestilizada. Os exports
legacy permanecem porque os 6 primitivos em `src/ui/*` ainda os consomem — sua migracao/limpeza
e da fase 25.

Output: `src/theme.ts` com a nova API de tema + exports legacy iOS intactos + `@expo-google-fonts/nunito`
e `expo-font` instalados.

Escopo: SOMENTE o modulo de tokens e as dependencias. Este plano NAO toca `src/App.tsx`,
nao carrega a fonte em runtime e nao reestiliza nenhuma tela. NAO remove nem altera os
exports legacy iOS.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-sistema-de-tema-salvia-creme/22-CONTEXT.md
@.claude/skills/sketch-findings-fala/references/fundamentos-visuais.md
@.claude/skills/sketch-findings-fala/sources/themes/default.css
@.claude/skills/sketch-findings-fala/sources/themes/terracota.css
@.claude/skills/sketch-findings-fala/sources/themes/sereno-escuro.css
@src/theme.ts

<interfaces>
<!-- Estado atual de src/theme.ts (a ser ESTENDIDO, nao substituido). -->
<!-- Exports atuais consumidos por src/App.tsx (import na linha 47): -->
<!--   import { CHILD_GRID_COLUMNS, colors, shadows, spacing, typography } from './theme'; -->
<!-- Exports atuais consumidos pelos 6 primitivos em src/ui/ (IOSBottomSheet, IOSButton, -->
<!--   IOSCard, IOSListRow, IOSListSection, IOSSectionHeader). -->

ATENCAO CRITICA — DOIS conjuntos de exports lado a lado:

Os 6 arquivos de `src/ui/*` consomem chaves de cor/tipografia/raio iOS que NAO
existem na paleta Salvia & Creme. Chaves iOS efetivamente usadas (confirmadas
por grep em src/ui/*):
  colors:     systemBlue, label, secondaryLabel, tertiaryLabel, separator,
              systemBackground, systemGray3, destructive, fill, tertiaryFill,
              secondarySystemGroupedBackground
  typography: footnote, caption1, headline, body
  radii:      md, lg, xl, pill
  spacing:    xs, sm, md, lg, xl
  shadows:    sm
Alem disso src/App.tsx consome `colors.tertiaryLabel` em 5 sites de TextInput.

A nova paleta Salvia & Creme NAO tem `systemBlue`, `label`, `separator`,
`radii.pill`, etc. Portanto os exports legacy NAO PODEM ser re-apontados para
`themes['default'].colors` (ou para typography/radii/spacing/shadows da nova
API) — isso quebraria `tsc --noEmit` em src/ui/* e nos 5 sites de App.tsx, e
o criterio "npm run lint passa" ficaria insatisfeito.

REGRA OBRIGATORIA:
- Os exports legacy `colors`, `typography`, `radii`, `spacing`, `shadows`,
  `palette`, `tiles`, `CHILD_GRID_COLUMNS` permanecem como os objetos iOS
  ATUAIS, VERBATIM — copia literal do conteudo de hoje deste arquivo, sem
  qualquer mudanca de valor ou de chave. NAO re-apontar para a API nova.
- A nova API de tema "Salvia & Creme" (`Theme`, `ThemeName`, `themes`,
  `resolveTheme`, `NUNITO_FONT_MAP`, `fonts`) vive sob nomes NOVOS, num bloco
  separado do arquivo.
- Os dois conjuntos coexistem ate a limpeza da fase 25.
- O plano 02 migra App.tsx para a API nova; src/ui/* continua nos exports
  legacy ate a fase 25. NAO remova nem altere os exports legacy aqui.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Instalar dependencias da fonte Nunito</name>
  <read_first>
    - package.json (verificar versoes e ausencia de expo-font / @expo-google-fonts/nunito)
    - app.json (confirmar Expo SDK 54)
  </read_first>
  <files>package.json, package-lock.json</files>
  <action>
    Instalar as duas dependencias alinhadas ao Expo SDK 54 com:
    `npx expo install expo-font @expo-google-fonts/nunito`
    (usar `npx expo install`, NAO `npm install` puro — alinha versao ao SDK, padrao do
    script `fix:expo` do projeto).
    `expo-font` e a runtime de carregamento de fonte; `@expo-google-fonts/nunito` traz os
    assets das fontes Nunito (pesos 400/600/700/800) e o hook `useFonts`.
    Nao adicionar plugin em app.json — `@expo-google-fonts/*` carrega via `useFonts` em
    runtime, sem config plugin nem rebuild nativo.
  </action>
  <verify>
    <automated>node -e "const p=require('./package.json'); if(!p.dependencies['expo-font']||!p.dependencies['@expo-google-fonts/nunito']) process.exit(1)"</automated>
  </verify>
  <acceptance_criteria>
    - `package.json` > `dependencies` contem a chave `expo-font`.
    - `package.json` > `dependencies` contem a chave `@expo-google-fonts/nunito`.
    - `npm run lint` (`tsc --noEmit`) continua sem erros novos.
  </acceptance_criteria>
  <done>As duas dependencias aparecem em package.json e o lint passa.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Adicionar o sistema de 3 temas Salvia & Creme a src/theme.ts (exports legacy intactos)</name>
  <read_first>
    - src/theme.ts (conteudo completo atual — TODOS os exports legacy que devem ser mantidos verbatim)
    - .claude/skills/sketch-findings-fala/sources/themes/default.css (valores exatos do tema default)
    - .claude/skills/sketch-findings-fala/sources/themes/terracota.css (valores exatos do tema terracota)
    - .claude/skills/sketch-findings-fala/sources/themes/sereno-escuro.css (valores exatos do tema escuro)
    - .claude/skills/sketch-findings-fala/references/fundamentos-visuais.md (principios e anti-patterns)
  </read_first>
  <files>src/theme.ts</files>
  <action>
    Editar `src/theme.ts` ADICIONANDO a nova API de tema. Os exports legacy iOS NAO sao
    tocados. Estrutura final do arquivo: bloco legacy iOS (intacto) + bloco novo Salvia & Creme.

    === PARTE A — PRESERVAR OS EXPORTS LEGACY iOS VERBATIM ===

    Os seguintes exports JA EXISTEM no arquivo e DEVEM permanecer com EXATAMENTE os mesmos
    valores e chaves de hoje (copia literal — nao mudar nenhum hex, nenhuma chave):
      - `export const colors` — objeto iOS atual (systemBlue '#007AFF', label, secondaryLabel,
        tertiaryLabel, quaternaryLabel, fill, secondaryFill, tertiaryFill, quaternaryFill,
        systemBackground, secondarySystemBackground, tertiarySystemBackground,
        systemGroupedBackground, secondarySystemGroupedBackground, separator, opaqueSeparator,
        systemGray..systemGray6, destructive, success, warning, highContrastBackground,
        highContrastText).
      - `export const typography` — escala iOS atual (largeTitle..caption2 com fontWeight).
      - `export const radii` — { sm:8, md:12, lg:16, xl:20, pill:999 }.
      - `export const spacing` — { xs:4, sm:8, md:12, lg:16, xl:20, xxl:24 }.
      - `export const shadows` — sm/md/lg iOS atuais (shadowColor '#000').
      - `export const palette`, `export const tiles`, `export const CHILD_GRID_COLUMNS`.
    REGRA: NAO re-apontar nenhum desses para a nova API. NAO renomear. NAO remover. Eles
    seguem sendo o objeto iOS literal porque os 6 primitivos de `src/ui/*` e 5 sites de
    App.tsx dependem dessas chaves (systemBlue, label, separator, radii.pill, etc.) que a
    paleta Salvia & Creme nao possui. A limpeza desses exports e da fase 25.

    === PARTE B — ADICIONAR A NOVA API DE TEMA "SALVIA & CREME" (nomes novos) ===

    Adicionar um bloco novo no arquivo (apos os exports legacy), com:

    1. Tipo `ThemeName = 'default' | 'terracota' | 'sereno-escuro'`.

    2. Tipo `Theme` com estes sub-objetos (todos obrigatorios):
       - `name: ThemeName`
       - `isDark: boolean`
       - `colors`: objeto com EXATAMENTE estas chaves (portadas dos `--color-*` do CSS):
         `bg, bgSoft, surface, surface2, border, borderStrong, text, textMuted, textSoft,
         primary, primaryHover, primarySoft, primaryInk, generate, generateHover, generateSoft,
         generateInk, accent, accentHover, accentSoft, danger, dangerSoft, success, star`
       - `radii`: `{ sm: 10, md: 16, lg: 22, xl: 30, full: 9999 }` (iguais nos 3 temas)
       - `spacing`: `{ xs: 4, sm: 8, md: 12, lg: 16, xl: 20, xxl: 24, xxxl: 32 }` (iguais nos 3 temas)
       - `shadows`: `{ sm, md, lg }` cada um do tipo `Pick<ViewStyle,'shadowColor'|'shadowOffset'|'shadowOpacity'|'shadowRadius'|'elevation'>`
       - `typography`: escala com `fontFamily` Nunito (ver passo 5).
       NOTA: este tipo `Theme` tem chaves PROPRIAS e e SEPARADO dos exports legacy. Nao ha
       conflito de nome — o tipo `Theme` e novo; os legacy sao consts, nao tipos.

    3. Valores de cor — portar LITERALMENTE dos arquivos CSS (so hex, sem `var()`):
       TEMA default (de default.css):
         bg #F1EBDD, bgSoft #E8E0CD, surface #FCFAF4, surface2 #F5F0E3, border #E2D8C2,
         borderStrong #CABD9D, text #3B362D, textMuted #8B8372, textSoft #B3AA94,
         primary #6F9D86, primaryHover #5C8872, primarySoft #DBE6DE, primaryInk #2E4A3E,
         generate #E0A24E, generateHover #CD9140, generateSoft #F6E6C7, generateInk #5B3F12,
         accent #D08A63, accentHover #BC7853, accentSoft #F0DCCE, danger #C57E6F,
         dangerSoft #EFDBD4, success #7FA982, star #E2B24C. isDark: false.
       TEMA terracota (de terracota.css):
         bg #F0E6D8, bgSoft #E6D8C5, surface #FDFAF3, surface2 #F5EBDA, border #E5D4BD,
         borderStrong #CFB896, text #3F352B, textMuted #91806C, textSoft #B8A88F,
         primary #C9805C, primaryHover #B56E4C, primarySoft #F0DDCE, primaryInk #5C3725,
         generate #DDA04B, generateHover #CA8F3E, generateSoft #F5E4C3, generateInk #5B3F12,
         accent #6F9D86, accentHover #5C8872, accentSoft #DBE6DE, danger #BF6A5C,
         dangerSoft #EFD6CF, success #7FA982, star #E2B24C. isDark: false.
       TEMA sereno-escuro (de sereno-escuro.css):
         bg #211F1B, bgSoft #1A1916, surface #2C2A24, surface2 #34322B, border #423F36,
         borderStrong #565244, text #F0EADC, textMuted #A9A18C, textSoft #7C7565,
         primary #8FBBA3, primaryHover #A2C9B3, primarySoft #33453C, primaryInk #EAF3EC,
         generate #E8B468, generateHover #F0C17E, generateSoft #4A3B22, generateInk #FBEFD6,
         accent #E0A07C, accentHover #EBAE8C, accentSoft #46362C, danger #D89384,
         dangerSoft #45302B, success #8FBBA3, star #E8C572. isDark: true.

    4. Sombras — converter os `--shadow-*` CSS para o formato RN:
       Temas default e terracota (tinta quente rgba(74,63,43,...)):
         sm: shadowColor '#4A3F2B', shadowOffset {width:0,height:1}, shadowOpacity 0.07, shadowRadius 3, elevation 1
         md: shadowColor '#4A3F2B', shadowOffset {width:0,height:6}, shadowOpacity 0.09, shadowRadius 16, elevation 3
         lg: shadowColor '#4A3F2B', shadowOffset {width:0,height:16}, shadowOpacity 0.13, shadowRadius 34, elevation 8
       Tema sereno-escuro (sombra preta mais densa, de sereno-escuro.css):
         sm: shadowColor '#000000', shadowOffset {width:0,height:1}, shadowOpacity 0.30, shadowRadius 3, elevation 1
         md: shadowColor '#000000', shadowOffset {width:0,height:6}, shadowOpacity 0.38, shadowRadius 16, elevation 3
         lg: shadowColor '#000000', shadowOffset {width:0,height:16}, shadowOpacity 0.46, shadowRadius 34, elevation 8

    5. Tipografia Nunito — definir uma constante `fonts` com os 4 pesos da Nunito,
       usando exatamente os nomes de familia que `@expo-google-fonts/nunito` exporta
       (chaves a registrar via useFonts no plano 02):
         regular: 'Nunito_400Regular'
         semibold: 'Nunito_600SemiBold'
         bold: 'Nunito_700Bold'
         extrabold: 'Nunito_800ExtraBold'
       O sub-objeto `typography` de cada tema reusa a MESMA escala (varia so cor via colors):
         largeTitle: { fontSize: 30, lineHeight: 38, fontFamily: fonts.extrabold }
         title1:     { fontSize: 24, lineHeight: 30, fontFamily: fonts.extrabold }
         title2:     { fontSize: 20, lineHeight: 26, fontFamily: fonts.bold }
         title3:     { fontSize: 18, lineHeight: 24, fontFamily: fonts.bold }
         headline:   { fontSize: 17, lineHeight: 22, fontFamily: fonts.bold }
         body:       { fontSize: 17, lineHeight: 23, fontFamily: fonts.regular }
         callout:    { fontSize: 16, lineHeight: 21, fontFamily: fonts.regular }
         subheadline:{ fontSize: 15, lineHeight: 20, fontFamily: fonts.semibold }
         footnote:   { fontSize: 13, lineHeight: 18, fontFamily: fonts.regular }
         caption1:   { fontSize: 12, lineHeight: 16, fontFamily: fonts.semibold }
         caption2:   { fontSize: 11, lineHeight: 14, fontFamily: fonts.regular }
       Cada token tipografico e do tipo `Pick<TextStyle,'fontSize'|'lineHeight'|'fontFamily'>`.
       NAO usar `fontWeight` — peso vem da familia (Nunito tem arquivos por peso).

    6. Exportar o registro e o resolvedor (nomes NOVOS):
       `export const themes: Record<ThemeName, Theme> = { 'default': ..., 'terracota': ..., 'sereno-escuro': ... };`
       `export function resolveTheme(name: ThemeName): Theme { return themes[name] ?? themes['default']; }`
       Exportar tambem `export const NUNITO_FONT_MAP` — objeto pronto para passar ao
       `useFonts` do plano 02: `{ Nunito_400Regular, Nunito_600SemiBold, Nunito_700Bold, Nunito_800ExtraBold }`
       importados de `@expo-google-fonts/nunito`. (Importar os 4 pesos no topo do arquivo.)
       Exportar `export type { Theme, ThemeName };` e `export const fonts`.

    7. NAO TOCAR OS EXPORTS LEGACY. Confirmar que `colors`, `typography`, `radii`, `spacing`,
       `shadows`, `palette`, `tiles`, `CHILD_GRID_COLUMNS` seguem com os mesmos valores e
       chaves de hoje. Eles NAO viram alias da nova API. Adicionar/atualizar um comentario:
       "Legacy iOS (v5) — consumido por src/ui/*; removido na fase 25. NAO re-apontar."

    8. Atualizar o comentario de cabecalho do arquivo para descrever que o arquivo agora
       contem DOIS sistemas: (a) o design system iOS legacy (exports antigos) e (b) o
       sistema "Salvia & Creme" novo (`Theme`, `themes`, `resolveTheme`, `NUNITO_FONT_MAP`,
       `fonts`). Texto em pt-BR.

    Anti-patterns a evitar na NOVA API (de fundamentos-visuais.md): nenhum `#007AFF`,
    nenhum `#000000` em token de TEXTO da nova API (texto e quase-preto quente), nenhuma
    sombra cinza-azulada. (O `#007AFF` no objeto legacy `colors` e esperado e permitido.)
  </action>
  <verify>
    <automated>npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `npm run lint` (`tsc --noEmit`) passa sem erros — incluindo src/ui/* (6 arquivos) e
      src/App.tsx, que continuam consumindo os exports legacy intactos.
    - `grep "export const themes" src/theme.ts` encontra a linha.
    - `grep "export function resolveTheme" src/theme.ts` encontra a linha.
    - `grep "export const NUNITO_FONT_MAP" src/theme.ts` encontra a linha.
    - `grep "#6F9D86" src/theme.ts` encontra (cor salvia / primary do tema default da nova API).
    - `grep "#C9805C" src/theme.ts` encontra (primary do tema terracota).
    - `grep "#211F1B" src/theme.ts` encontra (bg do tema sereno-escuro).
    - `grep "Nunito_400Regular" src/theme.ts` encontra.
    - `grep "Nunito_800ExtraBold" src/theme.ts` encontra.
    - `grep "systemBlue: '#007AFF'" src/theme.ts` encontra — o objeto legacy `colors`
      iOS permanece VERBATIM (NAO re-apontado para a nova API).
    - `grep "pill: 999" src/theme.ts` encontra — `radii` legacy intacto (chave `pill`
      que a nova API nao possui).
    - Os exports legacy `colors`, `typography`, `radii`, `spacing`, `shadows`, `palette`,
      `tiles`, `CHILD_GRID_COLUMNS` continuam presentes como objetos iOS (grep encontra cada um).
  </acceptance_criteria>
  <done>
    src/theme.ts contem DOIS sistemas: os exports legacy iOS verbatim (colors com systemBlue,
    typography com fontWeight, radii com pill, etc.) E a nova API Salvia & Creme (Theme, themes,
    resolveTheme, NUNITO_FONT_MAP, fonts); lint passa em todo o projeto (src/ui/* incluso).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (nenhuma) | Fase exclusivamente visual — sem novas fronteiras de confianca |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-01 | (n/a) | src/theme.ts, package.json | accept | Sem superficie de ataque nova — fase exclusivamente visual (tokens, fonte, tema); nenhuma mudanca em auth, rede, persistencia de dados sensiveis ou entrada de usuario. As dependencias adicionadas (@expo-google-fonts/nunito, expo-font) sao pacotes oficiais Expo de assets de fonte. |
</threat_model>

<verification>
- `npm run lint` sem erros (src/ui/* e src/App.tsx compilam contra os exports legacy intactos).
- `npm run test` sem novas falhas (alem das 4 pre-existentes herdadas).
- src/theme.ts compila e exporta a API de tema nova + os exports legacy iOS verbatim.
</verification>

<success_criteria>
- src/theme.ts contem 3 temas completos com valores portados de sources/themes/*.css.
- Nenhum token da NOVA API expoe o azul iOS #007AFF.
- Os exports legacy iOS (`colors`, `typography`, `radii`, `spacing`, `shadows`, `palette`,
  `tiles`, `CHILD_GRID_COLUMNS`) permanecem verbatim, sem re-apontamento.
- Tokens de fonte Nunito (4 pesos) presentes; NUNITO_FONT_MAP exportado.
- expo-font e @expo-google-fonts/nunito instalados.
- Build de tipos (tsc) limpo em todo o projeto.
</success_criteria>

<output>
After completion, create `.planning/phases/22-sistema-de-tema-salvia-creme/22-01-SUMMARY.md`
</output>
</content>
</invoke>
