---
phase: 24-configuracoes-agrupadas-com-drill-down
plan: 04
type: execute
wave: 4
depends_on: [24-01, 24-02, 24-03]
files_modified:
  - src/App.tsx
autonomous: false
requirements: [CFG-01, CFG-02, CFG-03]
tags: [config, drill-down, cuidador, senha, chave-ia, gating, salvia-creme]

must_haves:
  truths:
    - "A sub-tela 'Senha' apresenta o estado correto: criar (needsAdminSetup), entrar (!isAdmin), trocar (isAdmin) — cada estado em seu proprio card com inputs respiraveis."
    - "A sub-tela 'Chave da IA' tem o input e o botao 'Salvar chave da IA' separados do bloco de senha."
    - "O botao 'Sair do modo cuidador' aparece como linha-perigo (terracota/danger) na home do grupo Cuidador quando isAdmin === true, alem de permanecer dentro da sub-tela de senha como fallback."
    - "Os handlers de auth permanecem VERBATIM: handleAdminLogin, handleSaveAdminPassword e handleUpdateAdminPassword continuam chamando setIsConfigModalOpen(false) em sucesso — o sheet FECHA apos login/criar senha/atualizar senha. Ao reabrir Ajustes, a HOME ja esta desbloqueada (cadeados sumiram porque isAdmin === true)."
    - "Cuidador deslogado tocando 'Chave da IA' na home e direcionado primeiro ao gate de senha (defesa em camadas — gate principal em openConfigRoute, gate defensivo no render da chave-ia)."
  artifacts:
    - path: "src/App.tsx"
      provides: "Sub-tela 'senha' restilizada com 3 ramos (criar / entrar / trocar) tokenizados e botao 'Sair' como danger."
      contains: "configRoute === 'senha'"
    - path: "src/App.tsx"
      provides: "Sub-tela 'chave-ia' restilizada como secao propria com aviso e gate defensivo."
      contains: "configRoute === 'chave-ia'"
  key_links:
    - from: "Sub-tela 'senha' branch needsAdminSetup"
      to: "handleSaveAdminPassword"
      via: "botao 'Criar senha e entrar'"
      pattern: "handleSaveAdminPassword"
    - from: "Sub-tela 'senha' branch !isAdmin"
      to: "handleAdminLogin"
      via: "botao 'Entrar'"
      pattern: "handleAdminLogin"
    - from: "Sub-tela 'senha' branch isAdmin"
      to: "handleUpdateAdminPassword / handleAdminLogout"
      via: "botoes 'Atualizar senha' / 'Sair do modo cuidador'"
      pattern: "handleUpdateAdminPassword|handleAdminLogout"
    - from: "Sub-tela 'chave-ia'"
      to: "handleSaveAiKey / setAiApiKeyInput"
      via: "input + botao 'Salvar chave da IA'"
      pattern: "handleSaveAiKey"
---

<objective>
Restilizar e finalizar as sub-telas do grupo "Cuidador": Senha (com 3 estados —
criar / entrar / trocar) e Chave da IA. Aplicar o mesmo padrao de drill-down dos
planos 24-02/03 (cabecalho UPPERCASE muted, cards Salvia & Creme, inputs
respiraveis, botoes tokenizados). O botao "Sair do modo cuidador" aparece como
linha-perigo na home do grupo Cuidador (ja preparada no plano 24-01) e
PERMANECE como botao danger dentro da sub-tela de senha (estado isAdmin), como
fallback.

A LOGICA de autenticacao e PRESERVADA VERBATIM: `hashSecret`, `adminPasswordHash`,
`needsAdminSetup`, `handleAdminLogin`, `handleSaveAdminPassword`,
`handleUpdateAdminPassword`, `handleAdminLogout`, `handleSaveAiKey`,
`aiApiKeyInput`, `setAiApiKeyInput`, `adminPassword`, `newAdminPassword`,
`confirmAdminPassword`. Os handlers `handleAdminLogin` (linha 465-478),
`handleSaveAdminPassword` (linha 480-498) e `handleUpdateAdminPassword`
(linha 500-521) continuam chamando `setIsConfigModalOpen(false)` em sucesso —
o sheet FECHA apos login/criar senha/atualizar senha. Esse comportamento legado
e preservado verbatim. Nenhuma chamada nova de rede, nenhuma nova chave em
`STORAGE_KEYS`, nenhuma nova superficie de ataque.

Purpose: fechar CFG-01 + CFG-02 + CFG-03 visualmente para o grupo Cuidador, e
encerrar a Phase 24 com um checkpoint humano completo cobrindo os 3 grupos.
Output: 2 sub-telas restilizadas + checkpoint humano de regressao da Phase 24.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/24-configuracoes-agrupadas-com-drill-down/24-CONTEXT.md
@.planning/phases/24-configuracoes-agrupadas-com-drill-down/24-01-PLAN.md
@.planning/phases/24-configuracoes-agrupadas-com-drill-down/24-02-PLAN.md
@.planning/phases/24-configuracoes-agrupadas-com-drill-down/24-03-PLAN.md
@.claude/skills/sketch-findings-fala/references/configuracoes.md
@.claude/skills/sketch-findings-fala/references/fundamentos-visuais.md
@src/theme.ts
@src/App.tsx

<interfaces>
<!-- Contratos a consumir VERBATIM. NAO alterar nenhum handler de auth. -->

```ts
const [adminPassword, setAdminPassword] = useState('');
const [newAdminPassword, setNewAdminPassword] = useState('');
const [confirmAdminPassword, setConfirmAdminPassword] = useState('');
const [aiApiKeyInput, setAiApiKeyInput] = useState(DEFAULT_AI_API_KEY);

const needsAdminSetup = !adminPasswordHash;

// linha 465-478 — em sucesso: setIsAdmin(true); setIsConfigModalOpen(false); resetConfigFields();
const handleAdminLogin = useCallback(() => { /* ... */ });

// linha 480-498 — em sucesso: setIsAdmin(true); setIsConfigModalOpen(false); resetConfigFields();
const handleSaveAdminPassword = useCallback(async () => { /* ... */ });

// linha 500-521 — em sucesso: setIsConfigModalOpen(false); resetConfigFields();
const handleUpdateAdminPassword = useCallback(async () => { /* ... */ });

const handleAdminLogout = useCallback(() => { /* setIsAdmin(false) */ });
const handleSaveAiKey = useCallback(async () => { /* salva aiApiKeyInput em settings */ });
```

COMPORTAMENTO LEGADO VERBATIM (NAO ALTERAR): os 3 handlers de auth fecham o
sheet em sucesso (`setIsConfigModalOpen(false)`). UX esperada pos-sucesso:
sheet fecha; ao reabrir Ajustes, `resetConfigFields` ja deixou `configRoute === 'home'`,
entao o usuario cai na HOME desbloqueada (cadeados sumiram porque
`isAdmin === true`).

Tokens disponiveis (criados nos planos 24-02 e 24-03):
- `drillSectionTitle`, `drillSectionCard`, `drillFieldLabel`, `drillFieldHint`,
  `drillPrimaryButton`, `drillPrimaryButtonText`, `drillSecondaryButton`,
  `drillSecondaryButtonText`, `drillDangerButton`, `drillDangerButtonText`,
  `drillInlineInput`, `drillEmptyHint`, `drillToggleRow`.

Token NOVO (a adicionar): `drillPasswordInput` — input full-width para senhas.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Restilizar sub-tela 'Senha' (3 ramos: criar / entrar / trocar)</name>
  <files>src/App.tsx</files>
  <read_first>
    - src/App.tsx bloco `configRoute === 'senha'` (do plano 24-01) — espelha o antigo `configSection === 'seguranca'` linhas 3631-3741, exceto o sub-bloco Chave da IA (que foi extraido para 'chave-ia')
    - src/App.tsx handlers de auth (linhas ~465-540) — handlers preservados VERBATIM, INCLUINDO o setIsConfigModalOpen(false) em sucesso
    - src/App.tsx resetConfigFields (linha ~452)
  </read_first>
  <action>
    1. Adicionar em `makeStyles(theme)` o token novo:
       ```ts
       drillPasswordInput: {
         ...theme.typography.body,
         color: theme.colors.text,
         backgroundColor: theme.colors.surface2,
         borderRadius: theme.radii.md,
         borderWidth: 1,
         borderColor: theme.colors.border,
         paddingVertical: theme.spacing.md,
         paddingHorizontal: theme.spacing.md
       }
       ```
    2. Substituir o JSX do bloco `configRoute === 'senha'` por:
       ```tsx
       {configRoute === 'senha' && (
         needsAdminSetup ? (
           <>
             <Text style={styles.drillSectionTitle}>CRIAR SENHA DO CUIDADOR</Text>
             <View style={styles.drillSectionCard}>
               <Text style={styles.drillFieldHint}>
                 Defina uma senha para proteger as configurações da criança. Apenas o cuidador deve saber.
               </Text>
               <TextInput
                 value={newAdminPassword}
                 onChangeText={setNewAdminPassword}
                 placeholder="Nova senha"
                 placeholderTextColor={theme.colors.textMuted}
                 secureTextEntry
                 style={styles.drillPasswordInput}
                 autoFocus
               />
               <TextInput
                 value={confirmAdminPassword}
                 onChangeText={setConfirmAdminPassword}
                 placeholder="Confirmar senha"
                 placeholderTextColor={theme.colors.textMuted}
                 secureTextEntry
                 style={styles.drillPasswordInput}
               />
               <Pressable
                 style={styles.drillPrimaryButton}
                 onPress={() => void handleSaveAdminPassword()}
                 accessibilityRole="button"
                 accessibilityLabel="Criar senha e entrar"
               >
                 <Text style={styles.drillPrimaryButtonText}>Criar senha e entrar</Text>
               </Pressable>
             </View>
           </>
         ) : !isAdmin ? (
           <>
             <Text style={styles.drillSectionTitle}>ENTRAR COMO CUIDADOR</Text>
             <View style={styles.drillSectionCard}>
               <Text style={styles.drillFieldHint}>
                 Informe a senha para gerenciar favoritos, símbolos pessoais, frases e configurações avançadas.
               </Text>
               <TextInput
                 value={adminPassword}
                 onChangeText={setAdminPassword}
                 placeholder="Senha do cuidador"
                 placeholderTextColor={theme.colors.textMuted}
                 secureTextEntry
                 style={styles.drillPasswordInput}
                 autoFocus
                 onSubmitEditing={handleAdminLogin}
               />
               <Pressable
                 style={styles.drillPrimaryButton}
                 onPress={handleAdminLogin}
                 accessibilityRole="button"
                 accessibilityLabel="Entrar como cuidador"
               >
                 <Text style={styles.drillPrimaryButtonText}>Entrar</Text>
               </Pressable>
             </View>
           </>
         ) : (
           <>
             <Text style={styles.drillSectionTitle}>ALTERAR SENHA</Text>
             <View style={styles.drillSectionCard}>
               <TextInput
                 value={adminPassword}
                 onChangeText={setAdminPassword}
                 placeholder="Senha atual"
                 placeholderTextColor={theme.colors.textMuted}
                 secureTextEntry
                 style={styles.drillPasswordInput}
               />
               <TextInput
                 value={newAdminPassword}
                 onChangeText={setNewAdminPassword}
                 placeholder="Nova senha"
                 placeholderTextColor={theme.colors.textMuted}
                 secureTextEntry
                 style={styles.drillPasswordInput}
               />
               <TextInput
                 value={confirmAdminPassword}
                 onChangeText={setConfirmAdminPassword}
                 placeholder="Confirmar nova senha"
                 placeholderTextColor={theme.colors.textMuted}
                 secureTextEntry
                 style={styles.drillPasswordInput}
               />
               <Pressable
                 style={styles.drillPrimaryButton}
                 onPress={() => void handleUpdateAdminPassword()}
                 accessibilityRole="button"
                 accessibilityLabel="Atualizar senha"
               >
                 <Text style={styles.drillPrimaryButtonText}>Atualizar senha</Text>
               </Pressable>
             </View>

             <Text style={styles.drillSectionTitle}>SESSÃO</Text>
             <View style={styles.drillSectionCard}>
               <Text style={styles.drillFieldHint}>
                 Você está como cuidador. Para devolver o controle à criança, encerre a sessão.
               </Text>
               <Pressable
                 style={styles.drillDangerButton}
                 onPress={() => {
                   handleAdminLogout();
                   setIsConfigModalOpen(false);
                   resetConfigFields();
                 }}
                 accessibilityRole="button"
                 accessibilityLabel="Sair do modo cuidador"
               >
                 <Text style={styles.drillDangerButtonText}>Sair do modo cuidador</Text>
               </Pressable>
             </View>
           </>
         )
       )}
       ```
    3. Confirmar via leitura visual que NAO ha mais nenhum `styles.modalInput`,
       `styles.modalButtonPrimary`, `styles.modalButtonDanger`,
       `styles.modalSectionTitle`, `styles.modalHint` neste bloco; e que NENHUM
       handler de auth foi renomeado ou re-implementado. Os handlers
       `handleAdminLogin`, `handleSaveAdminPassword` e `handleUpdateAdminPassword`
       continuam chamando `setIsConfigModalOpen(false)` em sucesso — esse
       comportamento legado e PRESERVADO VERBATIM.
  </action>
  <verify>
    <automated>cd "C:/Users/eduau/OneDrive/Área de Trabalho/fala" && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "drillPasswordInput:" src/App.tsx` retorna 1.
    - Dentro de `configRoute === 'senha'`:
      - aparecem `needsAdminSetup`, `!isAdmin`, `isAdmin`
      - aparecem `handleSaveAdminPassword`, `handleAdminLogin`,
        `handleUpdateAdminPassword`, `handleAdminLogout`
      - aparecem `adminPassword`, `newAdminPassword`, `confirmAdminPassword`
      - aparece `setIsConfigModalOpen(false)` no onPress de "Sair"
    - Bloco senha NAO contem `styles.modalInput`, `styles.modalButtonPrimary`,
      `styles.modalButtonDanger`, `styles.modalSectionTitle`, `styles.modalHint`.
    - Handlers `handleAdminLogin` (linhas 465-478), `handleSaveAdminPassword`
      (linhas 480-498) e `handleUpdateAdminPassword` (linhas 500-521) NAO foram
      modificados — continuam contendo `setIsConfigModalOpen(false)` em sucesso.
    - `npm run lint` passa.
  </acceptance_criteria>
  <done>Sub-tela 'senha' restilizada nos 3 estados com botoes tokenizados; auth flow verbatim (handlers ainda fecham o sheet em sucesso); Sair = danger.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Restilizar sub-tela 'Chave da IA' com gate defensivo</name>
  <files>src/App.tsx</files>
  <read_first>
    - src/App.tsx bloco `configRoute === 'chave-ia'` (do plano 24-01)
    - src/App.tsx handler handleSaveAiKey + aiApiKeyInput + setAiApiKeyInput
  </read_first>
  <action>
    Substituir o JSX do bloco `configRoute === 'chave-ia'` por:
    ```tsx
    {configRoute === 'chave-ia' && (
      !isAdmin ? (
        <Text style={styles.drillEmptyHint}>Entre como cuidador para alterar a chave da IA.</Text>
      ) : (
        <>
          <Text style={styles.drillSectionTitle}>CHAVE DA IA (GEMINI)</Text>
          <View style={styles.drillSectionCard}>
            <Text style={styles.drillFieldHint}>
              A chave do `.env` é usada por padrão. Você pode substituir por outra aqui — fica salva apenas neste dispositivo.
            </Text>
            <TextInput
              value={aiApiKeyInput}
              onChangeText={setAiApiKeyInput}
              placeholder="EXPO_PUBLIC_GOOGLE_AI_API_KEY"
              placeholderTextColor={theme.colors.textMuted}
              autoCapitalize="none"
              autoCorrect={false}
              style={styles.drillPasswordInput}
            />
            <Pressable
              style={styles.drillPrimaryButton}
              onPress={() => void handleSaveAiKey()}
              accessibilityRole="button"
              accessibilityLabel="Salvar chave da IA"
            >
              <Text style={styles.drillPrimaryButtonText}>Salvar chave da IA</Text>
            </Pressable>
          </View>
        </>
      )
    )}
    ```
    Confirmar que `handleSaveAiKey` continua sendo apenas chamado aqui (e nao
    foi duplicado ou renomeado). O gate defensivo `!isAdmin ? hint : editor` e
    redundante com o gating principal de `openConfigRoute` (plano 24-01) — mantido
    por design (defesa em camadas).
  </action>
  <verify>
    <automated>cd "C:/Users/eduau/OneDrive/Área de Trabalho/fala" && npm run lint &amp;&amp; npm run test -- --runInBand</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "configRoute === 'chave-ia'" src/App.tsx` retorna 1.
    - Dentro do bloco chave-ia: aparece `handleSaveAiKey`, `aiApiKeyInput`,
      `setAiApiKeyInput`, e o ramo `!isAdmin` mostra `drillEmptyHint`.
    - `grep -c "handleSaveAiKey" src/App.tsx` retorna 2 (uma definicao + uma chamada).
    - Bloco chave-ia NAO contem `styles.modalInput`, `styles.modalButtonPrimary`.
    - `grep "fontFamily: '" src/App.tsx` retorna 0.
    - `npm run lint` passa; `npm run test` 22/26.
  </acceptance_criteria>
  <done>Sub-tela 'chave-ia' restilizada com gate defensivo; handler preservado.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Checkpoint humano — Phase 24 completa nos 3 grupos</name>
  <what-built>
    Phase 24 inteira: home agrupada em 3 grupos (CFG-01), drill-down para sub-telas
    dedicadas com botao voltar e sem listas densas (CFG-02), gating por grupo com
    cadeados visuais e redirect para senha (CFG-03). Tema Salvia & Creme aplicado
    em todas as sub-telas via tokens `theme.*`. Fluxo de auth preservado verbatim
    — incluindo o comportamento legado dos 3 handlers de auth de fechar o sheet
    em sucesso (`setIsConfigModalOpen(false)` em handleAdminLogin /
    handleSaveAdminPassword / handleUpdateAdminPassword).
  </what-built>
  <how-to-verify>
    1. `npm run start` em iOS/Android.
    2. Abrir Ajustes -> abre na HOME agrupada (APP / CONTEÚDO DA CRIANÇA /
       CUIDADOR).
    3. Cuidador DESLOGADO:
       - APP: 3 linhas sem cadeado, todas drill-down funcionando (Voz,
         Acessibilidade, Aparência). Cada sub-tela mostra titulo grande, cards
         creme com cabecalho UPPERCASE muted, espaco respiravel.
       - CONTEÚDO DA CRIANÇA: 6 linhas com 🔒; tocar QUALQUER UMA leva ao gate
         "Senha do cuidador" (criar ou entrar, dependendo de needsAdminSetup).
       - CUIDADOR: linha "Criar senha" (ou "Entrar como cuidador") sem cadeado;
         "Chave da IA" com 🔒. NAO existe linha "Sair".
    4. Criar uma senha ou logar. COMPORTAMENTO ESPERADO (handlers legados
       preservados verbatim): apos sucesso o SHEET FECHA automaticamente
       (handleSaveAdminPassword / handleAdminLogin chamam
       `setIsConfigModalOpen(false)`). Reabrir Ajustes -> a HOME ja esta
       DESBLOQUEADA: sem cadeados nos grupos "Conteúdo da criança" e "Cuidador",
       e aparece a linha "Sair do modo cuidador" em terracota/danger no grupo
       Cuidador.
    5. Drill-down completo dos 6 itens de "Conteudo da crianca":
       - Vocabulário core: reordenar com ↑/↓ (chips 32x32 respiraveis), adicionar
         palavra nova, remover, restaurar padrao.
       - Frases prontas: adicionar, editar (icone ✎), salvar (OK), cancelar (✕),
         remover, limpar historico (mostra contagem).
       - Símbolos pessoais: botoes Camera / Galeria aparecem em card (ambos
         desabilitados durante captura — pickerBusy); abrir o draft (apenas
         confirmar que abre — captura real e opcional). Galeria de simbolos
         pessoais: cada linha tem exatamente 3 acoes — 🎙 (gravar/regravar),
         🔇 SO quando existe audio gravado, ✕ (remover simbolo).
       - Categorias: adicionar, renomear, remover; sub-label "X simbolos" aparece.
       - Rotina do dia: editor legado funciona dentro do novo card (passos com
         dias preservados).
       - Cenas visuais: gerenciador legado funciona dentro do novo card.
    6. Drill-down do grupo Cuidador:
       - Senha (isAdmin): 2 cards — ALTERAR SENHA e SESSÃO. Trocar senha:
         preencher "Senha atual" + "Nova" + "Confirmar" + tocar "Atualizar
         senha". COMPORTAMENTO ESPERADO (handler legado verbatim): apos sucesso
         o SHEET FECHA (handleUpdateAdminPassword chama
         `setIsConfigModalOpen(false)`). Reabrir Ajustes mostra a HOME
         desbloqueada (isAdmin === true, sem cadeados).
       - Botao "Sair do modo cuidador" (danger): fecha o sheet e desloga
         (handleAdminLogout + setIsConfigModalOpen(false) + resetConfigFields).
       - Chave da IA: campo + botao "Salvar chave da IA" funcionam.
    7. Smoke test final da regressao:
       - "Sair" pelo botao danger -> volta para deslogado, cadeados voltam.
       - Fechar e reabrir o sheet -> sempre abre na HOME.
       - Trocar tema (Sereno Escuro) — toda a estrutura agrupada se mantem legivel
         no modo escuro calmo.
    8. Visual: nenhum azul iOS aparece nas sub-telas restilizadas. Listas densas
       (3 botoes minusculos) eliminadas em Vocabulario / Frases / Categorias.
       Rotina e Cenas continuam com editores legados (intencional — fora do
       escopo desta fase, ver Deferred em 24-CONTEXT.md).
  </how-to-verify>
  <resume-signal>
    Responda "approved" para fechar Phase 24, ou descreva problemas concretos
    (ex.: cadeados nao desaparecem ao reabrir apos login, sheet NAO fecha apos
    criar senha/login/trocar senha — isso seria regressao do comportamento
    legado, "Sair" nao desloga, input de senha nao recebe foco, regressao em
    editor X, cor errada em Y).
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| App rendering -> auth state | A sub-tela `chave-ia` mantem gate defensivo `!isAdmin ? hint : editor`, redundante mas explicito (gate principal em `openConfigRoute`). |
| User input (senhas, chave IA) -> AsyncStorage | Inputs continuam usando `secureTextEntry` para senhas; `handleSaveAdminPassword`, `handleUpdateAdminPassword`, `handleSaveAiKey` preservados verbatim. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-07 | S (Spoofing) | sub-tela 'senha' | mitigate | Inputs de senha continuam com `secureTextEntry`; handlers `handleAdminLogin`/`handleSaveAdminPassword`/`handleUpdateAdminPassword` validam via `hashSecret` (preservado verbatim, INCLUINDO o setIsConfigModalOpen(false) em sucesso). |
| T-24-08 | I (Information Disclosure) | sub-tela 'chave-ia' | mitigate | Gate defensivo `!isAdmin` no render; gate principal em `openConfigRoute`. Aviso pt-BR explicita que a chave fica apenas no dispositivo. |
| T-24-09 | T (Tampering) | persistencia | accept | Sem nova superficie de ataque — fluxo de auth (hashSecret, adminPasswordHash, setAdminPassword) preservado verbatim; apenas o invólucro visual e a navegacao mudam. Sem novas chamadas de rede, persistencia ou input handling. |
</threat_model>

<verification>
- `npm run lint` limpo; `npm run test` 22/26 (sem novas falhas).
- `grep "fontFamily: '" src/App.tsx` retorna 0 (VIS-02 segue fechado).
- Greps positivos:
  - `grep -n "drillPasswordInput:" src/App.tsx` retorna 1
  - `grep -c "configRoute === 'senha'" src/App.tsx` retorna 1
  - `grep -c "configRoute === 'chave-ia'" src/App.tsx` retorna 1
  - `grep -c "handleAdminLogin\|handleSaveAdminPassword\|handleUpdateAdminPassword\|handleAdminLogout\|handleSaveAiKey" src/App.tsx` retorna >= 9 (definicoes + chamadas)
  - Handlers `handleAdminLogin`, `handleSaveAdminPassword` e
    `handleUpdateAdminPassword` continuam contendo `setIsConfigModalOpen(false)`
    em sucesso (preservados verbatim).
- Checkpoint humano (Task 3) aprovado pelo usuario.
</verification>

<success_criteria>
- CFG-01 fechado: home de 3 grupos visivel e completamente coerente nos 3 grupos.
- CFG-02 fechado: nenhuma sub-tela mantem listas densas de "rotulo + 3
  botoezinhos espremidos" nas areas restilizadas (vocabulario / frases /
  categorias). Rotina e cenas continuam com editores legados dentro do novo
  card (intencional — Deferred).
- CFG-03 fechado: cuidador deslogado ve cadeados, tap em linha gated leva ao
  gate de senha; logado ve "Sair do modo cuidador" como danger.
- Fluxo de auth (setup / login / trocar senha / sair / chave-ia) preservado
  verbatim — handlers de auth continuam fechando o sheet em sucesso, e ao
  reabrir Ajustes o cuidador cai na HOME desbloqueada — validado por humano.
</success_criteria>

<output>
Apos completar, criar `.planning/phases/24-configuracoes-agrupadas-com-drill-down/24-04-SUMMARY.md`
e marcar a Phase 24 como concluida em `.planning/STATE.md` (transicao para Phase 25).
</output>
</content>
</invoke>