---
phase: 24-configuracoes-agrupadas-com-drill-down
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/App.tsx
autonomous: true
requirements: [CFG-01, CFG-02, CFG-03]
tags: [config, drill-down, navigation, gating, salvia-creme]

must_haves:
  truths:
    - "Ao abrir Ajustes, o cuidador ve 3 grupos (App / Conteudo da crianca / Cuidador), nao mais 10 abas planas."
    - "Tocar em qualquer linha da home leva para uma sub-tela dedicada com botao voltar."
    - "Cuidador deslogado: linhas do grupo App sao clicaveis; linhas dos outros 2 grupos mostram cadeado e abrem o fluxo de senha."
    - "Botao voltar no detalhe traz de volta a home agrupada sem fechar o sheet."
    - "Fechar o sheet e reabrir reseta o estado para 'home' (nao mantem o usuario na ultima sub-tela)."
    - "Apos login / criar senha / atualizar senha bem-sucedidos, os handlers de auth fecham o sheet (comportamento legado preservado); ao reabrir, a home mostra os 3 grupos desbloqueados."
  artifacts:
    - path: "src/App.tsx"
      provides: "Tipo ConfigRoute, estado configRoute, lista de grupos, componente ConfigGroupedHome e wrapper ConfigDetailScreen; remocao de ConfigSection/configSection/ConfigNavItem do fluxo de config (legacy local)."
      contains: "type ConfigRoute"
    - path: "src/App.tsx"
      provides: "Helper openConfigRoute(route) com gating: se cuidador deslogado e o grupo nao for 'App', abre route='senha' (ou direciona ao gate)."
      contains: "openConfigRoute"
  key_links:
    - from: "src/App.tsx (IOSBottomSheet config)"
      to: "ConfigGroupedHome"
      via: "renderiza ConfigGroupedHome quando configRoute === 'home'"
      pattern: "configRoute === 'home'"
    - from: "src/App.tsx (IOSBottomSheet config)"
      to: "ConfigDetailScreen"
      via: "renderiza wrapper de detalhe quando configRoute !== 'home'"
      pattern: "configRoute !== 'home'"
    - from: "Linha de grupo Conteudo da crianca / Cuidador (deslogado)"
      to: "openConfigRoute('senha')"
      via: "gating; cadeado visivel; tap chama openConfigRoute que redireciona"
      pattern: "!isAdmin"
---

<objective>
Estabelecer o shell agrupado e o sistema de drill-down dentro do `IOSBottomSheet` de
Ajustes. Substitui a barra horizontal de 10 abas planas (ConfigNavItem +
configSection) por uma home agrupada em 3 grupos (App / Conteudo da crianca /
Cuidador) e um wrapper de tela de detalhe com botao voltar. Implementa o gating
visual (cadeado nas linhas dos grupos protegidos quando o cuidador esta deslogado)
e a navegacao por troca de estado (sem React Navigation, coerente com o app
single-screen — CLAUDE.md).

Os corpos de detalhe (Voz, Acessibilidade, Aparencia, Vocabulario, Frases,
Simbolos, Categorias, Rotina, Cenas, Senha, Chave IA) continuam usando os
componentes/JSX existentes neste plano e sao movidos / restilizados pelos planos
24-02 / 24-03 / 24-04. Aqui criamos APENAS o shell e a infraestrutura de rota;
nas primeiras passagens, cada route renderiza o JSX legado do antigo
`configSection` correspondente, garantindo zero regressao funcional desde o
primeiro commit.

Purpose: viabilizar CFG-01 (home agrupada), CFG-02 (navegacao drill-down) e CFG-03
(gating por grupo) com tokens da paleta Salvia & Creme, mantendo TODA a logica de
auth (`hashSecret`, `adminPasswordHash`, `setAdminPassword`, `needsAdminSetup`)
verbatim — INCLUSIVE o comportamento dos handlers `handleAdminLogin`,
`handleSaveAdminPassword` e `handleUpdateAdminPassword` de fechar o sheet
(`setIsConfigModalOpen(false)`) apos sucesso. NAO tentar mudar esse comportamento.
Output: shell agrupado + drill-down funcional; cada route renderiza por enquanto o
mesmo conteudo do `configSection` legado (sera restilizado nos planos 24-02/03/04).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/24-configuracoes-agrupadas-com-drill-down/24-CONTEXT.md
@.claude/skills/sketch-findings-fala/SKILL.md
@.claude/skills/sketch-findings-fala/references/configuracoes.md
@.claude/skills/sketch-findings-fala/references/fundamentos-visuais.md
@src/theme.ts
@src/App.tsx
@src/ui/IOSBottomSheet.tsx
@CLAUDE.md

<interfaces>
<!-- Contratos extraidos diretamente do codigo. Use VERBATIM. -->

ConfigSection legado (em src/App.tsx linha 67) — sera REMOVIDO ao final:
```ts
type ConfigSection = 'perfil' | 'acessibilidade' | 'voz' | 'seguranca'
  | 'vocabulario' | 'frases' | 'simbolos' | 'categorias' | 'rotina' | 'cenas';
```

ConfigRoute NOVO (a criar, perto do antigo type):
```ts
type ConfigRoute =
  | 'home'
  | 'voz' | 'acessibilidade' | 'aparencia'
  | 'vocabulario' | 'frases' | 'simbolos' | 'categorias' | 'rotina' | 'cenas'
  | 'senha' | 'chave-ia';
```

Mapeamento ConfigSection legado -> ConfigRoute novo:
- 'voz' -> 'voz'
- 'acessibilidade' -> 'acessibilidade'  (tema do app + feedback visual)
- 'perfil'         -> 'aparencia'       (escala UI + grid columns)
- 'vocabulario' / 'frases' / 'simbolos' / 'categorias' / 'rotina' / 'cenas' -> mesmo nome
- 'seguranca' (login/setup/troca-senha + chave IA) -> dividido em 'senha' + 'chave-ia'

Auth state (em src/App.tsx, NAO alterar) — handlers VERBATIM:
```ts
const [isAdmin, setIsAdmin] = useState(false);
const [adminPasswordHash, setAdminPasswordHash] = useState<string | null>(null);
const needsAdminSetup = !adminPasswordHash;

// linha 465-478 — handleAdminLogin: em sucesso faz setIsAdmin(true);
//                  setIsConfigModalOpen(false); resetConfigFields();
const handleAdminLogin = useCallback(() => { /* ... */ }, ...);

// linha 480-498 — handleSaveAdminPassword: em sucesso faz setIsAdmin(true);
//                  setIsConfigModalOpen(false); resetConfigFields();
const handleSaveAdminPassword = useCallback(async () => { /* ... */ }, ...);

// linha 500-521 — handleUpdateAdminPassword: em sucesso faz
//                  setIsConfigModalOpen(false); resetConfigFields();
const handleUpdateAdminPassword = useCallback(async () => { /* ... */ }, ...);

// linha 534 — handleAdminLogout: setIsAdmin(false); setIsMenuOpen(false); showToast(...);
const handleAdminLogout = useCallback(() => { /* ... */ }, ...);

// IMPORTANTE: handleAdminLogin / handleSaveAdminPassword / handleUpdateAdminPassword
// chamam setIsConfigModalOpen(false) em sucesso — FECHAM o sheet. Esse e o
// comportamento legado correto e preservado VERBATIM. NAO MEXER. A UX esperada
// pos-sucesso e: sheet fecha; ao reabrir Ajustes, configRoute ja foi resetado
// para 'home' por resetConfigFields(), entao o cuidador ve a HOME com os 3
// grupos desbloqueados (cadeados sumiram porque isAdmin === true).
```

openConfigModal HOJE (em src/App.tsx linhas 459-463):
```ts
const openConfigModal = useCallback(() => {
  resetConfigFields();
  setConfigSection('seguranca');   // <-- esta linha sera REMOVIDA na Task 1
  setIsConfigModalOpen(true);
}, [resetConfigFields]);
```
A chamada `setConfigSection('seguranca')` vive AQUI dentro de `openConfigModal`,
NAO dentro dos handlers de auth. Apos a remocao, resetConfigFields (atualizado
para setar `configRoute === 'home'`) ja garante que abrir Ajustes cai na HOME.

IOSBottomSheet API (src/ui/IOSBottomSheet.tsx) — continua sendo o container:
```ts
interface IOSBottomSheetProps {
  visible: boolean;
  onRequestClose: () => void;
  title?: string;
  leftAction?: IOSBottomSheetAction;
  rightAction?: IOSBottomSheetAction;
  children: ReactNode;
  ...
}
```

Theme (src/theme.ts BLOCO 2) — Salvia & Creme:
- `theme.colors.bg / bgSoft / surface / surface2 / border / text / textMuted / textSoft / primary / primarySoft / primaryInk / danger`
- `theme.radii.lg` (22) para cartoes de grupo; `theme.radii.md` (16) para tiles
- `theme.spacing.*`; `theme.shadows.sm`; `theme.typography.headline / body / footnote / caption1` (Nunito)
- `StyleSheet.hairlineWidth` para hairline interna (cor `theme.colors.border`)

CSS de referencia (vencedor A) — em `.claude/skills/sketch-findings-fala/references/configuracoes.md`:
- `.sgroup-title` font-size 11, weight 800, uppercase, letter-spacing .6
- `.scard` background surface, border 1, radius lg, shadow sm, overflow hidden
- `.srow` flex row align center, gap 12, padding 13x14
- `.srow + .srow` border-top 1 hairline
- `.sicon` 38x38, radius 11, bg primarySoft
- `.srow.danger .slabel` color danger
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Trocar ConfigSection por ConfigRoute e remover ConfigNavItem do shell de config</name>
  <files>src/App.tsx</files>
  <read_first>
    - src/App.tsx linhas 60-80 (type ConfigSection)
    - src/App.tsx linhas 415-540 (estado isAdmin, adminPasswordHash, handlers de auth, resetConfigFields, openConfigModal)
    - src/App.tsx linhas 459-463 (openConfigModal — onde HOJE esta o setConfigSection('seguranca'))
    - src/App.tsx linhas 2600-2745 (bloco IOSBottomSheet "Ajustes": configSubheader, configNav, primeiros configSection)
    - src/App.tsx linhas 3819-3842 (componente ConfigNavItem)
    - src/App.tsx linha 1014 (uso externo de setConfigSection — chamada no fluxo de "criar simbolo a partir da camera" precisa virar setConfigRoute('simbolos'))
    - .claude/skills/sketch-findings-fala/references/configuracoes.md (Estrutura — Inicio + drill-down)
  </read_first>
  <action>
    1. Em src/App.tsx perto da linha 67, ADICIONAR um novo type ao lado de (NAO no lugar de ainda) ConfigSection:
       ```ts
       type ConfigRoute =
         | 'home'
         | 'voz' | 'acessibilidade' | 'aparencia'
         | 'vocabulario' | 'frases' | 'simbolos' | 'categorias' | 'rotina' | 'cenas'
         | 'senha' | 'chave-ia';
       ```
    2. SUBSTITUIR `const [configSection, setConfigSection] = useState<ConfigSection>('perfil');`
       (linha ~420) por
       `const [configRoute, setConfigRoute] = useState<ConfigRoute>('home');`
    3. Atualizar `resetConfigFields` (linhas 451-457) para chamar `setConfigRoute('home')` em vez
       de `setConfigSection('perfil')`. Manter o reset de `setAdminPassword('')`,
       `setNewAdminPassword('')`, `setConfirmAdminPassword('')`,
       `setAiApiKeyInput(DEFAULT_AI_API_KEY)` verbatim.
    4. Em `openConfigModal` (linhas 459-463), REMOVER a linha
       `setConfigSection('seguranca');`. Essa chamada vive AQUI, dentro de
       `openConfigModal` — NAO dentro dos handlers de auth, que NAO devem ser
       tocados. O `resetConfigFields` (atualizado no passo 3) ja garante que
       `configRoute === 'home'`. Apos a remocao, o corpo do callback fica:
       ```ts
       const openConfigModal = useCallback(() => {
         resetConfigFields();      // ja garante configRoute === 'home'
         setIsConfigModalOpen(true);
       }, [resetConfigFields]);
       ```
       Comportamento desejado da nova UX: abrir Ajustes sempre cai na HOME agrupada.
    5. NAO MEXER em `handleAdminLogin` (linhas 465-478), `handleSaveAdminPassword`
       (linhas 480-498) e `handleUpdateAdminPassword` (linhas 500-521). Esses 3
       handlers ja chamam `setIsConfigModalOpen(false)` + `resetConfigFields()` em
       sucesso — o sheet FECHA. Esse comportamento e preservado VERBATIM. Ao
       reabrir Ajustes apos sucesso, o usuario cai na HOME (3 grupos sem cadeado,
       pois `isAdmin === true`).
    6. Em src/App.tsx linha ~1014 (fluxo de "criar simbolo a partir da camera" no
       composer) substituir `setConfigSection('simbolos')` por
       `setConfigRoute('simbolos')`. Manter `setIsConfigModalOpen(true)`.
    7. Adicionar helper `openConfigRoute` logo apos os handlers de auth (~linha 540):
       ```ts
       const openConfigRoute = useCallback((route: ConfigRoute) => {
         // Grupo "App" (voz/acessibilidade/aparencia) e sempre livre.
         const APP_ROUTES: ConfigRoute[] = ['voz', 'acessibilidade', 'aparencia'];
         if (route === 'home') { setConfigRoute('home'); return; }
         if (!isAdmin && !APP_ROUTES.includes(route)) {
           // Cuidador deslogado tentando entrar em grupo protegido: enviar para o gate de senha.
           setConfigRoute('senha');
           return;
         }
         setConfigRoute(route);
       }, [isAdmin]);
       ```
    8. REMOVER a antiga declaracao `type ConfigSection = ...` (linha 67) e qualquer
       outra referencia residual a `configSection` / `setConfigSection` /
       `ConfigSection`. Apos esta task, `grep -n "configSection\|setConfigSection\|ConfigSection" src/App.tsx` deve retornar 0.
    9. REMOVER a funcao `ConfigNavItem` (linhas 3819-3842) — ela nao tera mais
       call site apos a task 2.
    10. NAO mexer ainda no JSX dentro do `<IOSBottomSheet visible={isConfigModalOpen}` —
       sera substituido pelas Tasks 2a/2b. Para nao quebrar o build entre tasks, dentro do
       sheet substituir `configSection === 'X'` por `configRoute === 'X'`
       seletivamente — basta usar `find/replace` literal do prefixo `configSection ===` por
       `configRoute ===`. As rotas legadas casam todas exceto duas: tratar separado:
         - `configSection === 'perfil'` -> `configRoute === 'aparencia'`
         - `configSection === 'seguranca'` -> `(configRoute === 'senha' || configRoute === 'chave-ia')`
       O `configNav` (View linha 2628-2689 com os ConfigNavItem) deve ser REMOVIDO
       inteiramente nesta task — sera substituido pela home agrupada na Task 2a. Apos
       remocao, o sheet provisoriamente nao tem navegacao visivel ate a Task 2a; o
       build deve compilar (`npm run lint`).
  </action>
  <verify>
    <automated>cd "C:/Users/eduau/OneDrive/Área de Trabalho/fala" && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "type ConfigSection" src/App.tsx` retorna 0.
    - `grep -n "ConfigSection\|configSection\|setConfigSection" src/App.tsx` retorna 0.
    - `grep -n "type ConfigRoute" src/App.tsx` retorna 1.
    - `grep -n "function ConfigNavItem" src/App.tsx` retorna 0.
    - `grep -n "configRoute" src/App.tsx` retorna >= 8 (estado + handlers + JSX condicionais).
    - `grep -n "openConfigRoute" src/App.tsx` retorna >= 1.
    - `grep -n "setConfigSection('seguranca')" src/App.tsx` retorna 0 (a linha foi removida de openConfigModal).
    - `handleAdminLogin`, `handleSaveAdminPassword`, `handleUpdateAdminPassword`
      continuam contendo `setIsConfigModalOpen(false)` (auth verbatim preservado).
    - `npm run lint` (tsc --noEmit) passa sem erros novos.
  </acceptance_criteria>
  <done>
    Type ConfigSection foi substituido por ConfigRoute, ConfigNavItem removido,
    helper openConfigRoute existe com gating, fluxo de auth intacto (handlers
    ainda fecham o sheet em sucesso). Build limpo.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2a: Tokens novos + ConfigGroupRow + ConfigGroupedHome (3 grupos)</name>
  <files>src/App.tsx</files>
  <read_first>
    - src/App.tsx linhas 2600-2745 (bloco IOSBottomSheet de Ajustes resultante da Task 1)
    - src/App.tsx linhas 4368-fim de makeStyles (entender o padrao de adicionar style keys que dependem de theme)
    - src/App.tsx linhas 5816-5990 (configSheet, configSubheader, configHeaderSubtitle, configModalContent, modalSectionTitle, modalHint — estilos atuais para inspirar tokens)
    - src/theme.ts BLOCO 2 (theme.colors, theme.radii.lg/md, theme.spacing, theme.shadows.sm, theme.typography)
    - .claude/skills/sketch-findings-fala/references/configuracoes.md (CSS Patterns sgroup-title, scard, srow, sicon, srow.danger)
    - .claude/skills/sketch-findings-fala/sources/004-tela-configuracoes/index.html (abrir no navegador para conferir a variante A; estilo iOS Ajustes — uma coisa por tela)
  </read_first>
  <action>
    1. Em `makeStyles(theme)` (linha ~4368, dentro do StyleSheet.create) adicionar os
       seguintes style keys novos (use tokens de tema; NAO hardcode cores):
       ```ts
       // Shell / home agrupada
       configHomeContent: { padding: theme.spacing.lg, gap: theme.spacing.xl },
       sGroupBlock: { gap: theme.spacing.sm },
       sGroupTitle: {
         ...theme.typography.caption1,
         color: theme.colors.textMuted,
         textTransform: 'uppercase',
         letterSpacing: 0.6,
         paddingHorizontal: theme.spacing.sm,
         paddingBottom: 6
       },
       sCard: {
         backgroundColor: theme.colors.surface,
         borderRadius: theme.radii.lg,
         borderWidth: 1,
         borderColor: theme.colors.border,
         overflow: 'hidden',
         ...theme.shadows.sm
       },
       sRow: {
         flexDirection: 'row',
         alignItems: 'center',
         gap: 12,
         paddingVertical: 13,
         paddingHorizontal: 14
       },
       sRowDivider: {
         borderTopWidth: StyleSheet.hairlineWidth,
         borderTopColor: theme.colors.border
       },
       sIcon: {
         width: 38, height: 38, borderRadius: 11,
         backgroundColor: theme.colors.primarySoft,
         alignItems: 'center', justifyContent: 'center'
       },
       sIconEmoji: { fontSize: 20 },
       sLabel: { flex: 1, ...theme.typography.body, color: theme.colors.text },
       sLabelDanger: { color: theme.colors.danger },
       sChevron: { ...theme.typography.headline, color: theme.colors.textMuted, marginLeft: 4 },
       sLockBadge: { ...theme.typography.caption1, color: theme.colors.textMuted, marginLeft: 4 }
       ```
       Manter os estilos legados (configSheet, configSubheader, configHeaderSubtitle,
       configNav*, configSection*, modal*) — eles continuam sendo usados nos planos
       24-02/03/04 e nos sub-corpos pre-restilizacao.
    2. Definir, ANTES do `return` do componente App.tsx (mesmo nivel que ConfigNavItem
       costumava estar — escopo de modulo), o componente `ConfigGroupRow`:
       ```tsx
       function ConfigGroupRow({ icon, label, onPress, locked = false, danger = false, first = false }: {
         icon: string; label: string; onPress: () => void;
         locked?: boolean; danger?: boolean; first?: boolean;
       }) {
         const styles = moduleStyles;
         return (
           <Pressable
             onPress={onPress}
             style={({ pressed }) => [styles.sRow, !first && styles.sRowDivider, pressed && { opacity: 0.6 }]}
             accessibilityRole="button"
             accessibilityLabel={`${label}${locked ? ' (bloqueado)' : ''}`}
           >
             <View style={styles.sIcon}><Text style={styles.sIconEmoji}>{icon}</Text></View>
             <Text style={[styles.sLabel, danger && styles.sLabelDanger]} numberOfLines={1}>{label}</Text>
             {locked ? <Text style={styles.sLockBadge}>🔒</Text> : null}
             {!danger ? <Text style={styles.sChevron}>›</Text> : null}
           </Pressable>
         );
       }
       ```
    3. REMOVER o bloco `<View style={styles.configSubheader}>` (linhas 2616-2620) e
       seu `<Text style={styles.configHeaderSubtitle}>` — o subheader generico ficou
       redundante com o sistema agrupado + drill-down.
    4. Dentro do `<IOSBottomSheet visible={isConfigModalOpen}>`, ENVELOPAR o conteudo
       atual em um branch condicional. Por enquanto, criar APENAS o branch HOME e
       deixar o ramo `else` mostrando temporariamente um `<View />` vazio com TODO.
       Esqueleto:
       ```tsx
       <View style={styles.configSheet}>
         {configRoute === 'home' ? (
           <ScrollView showsVerticalScrollIndicator={false} contentContainerStyle={styles.configHomeContent}>
             {/* Grupo APP — sempre clicavel */}
             <View style={styles.sGroupBlock}>
               <Text style={styles.sGroupTitle}>APP</Text>
               <View style={styles.sCard}>
                 <ConfigGroupRow icon="🔊" label="Voz" onPress={() => openConfigRoute('voz')} first />
                 <ConfigGroupRow icon="👁" label="Acessibilidade" onPress={() => openConfigRoute('acessibilidade')} />
                 <ConfigGroupRow icon="⚙" label="Aparência" onPress={() => openConfigRoute('aparencia')} />
               </View>
             </View>
             {/* Grupo CONTEUDO DA CRIANCA — gated */}
             <View style={styles.sGroupBlock}>
               <Text style={styles.sGroupTitle}>CONTEÚDO DA CRIANÇA</Text>
               <View style={styles.sCard}>
                 <ConfigGroupRow icon="🗣" label="Vocabulário core" locked={!isAdmin} onPress={() => openConfigRoute('vocabulario')} first />
                 <ConfigGroupRow icon="💬" label="Frases prontas"  locked={!isAdmin} onPress={() => openConfigRoute('frases')} />
                 <ConfigGroupRow icon="📷" label="Símbolos pessoais" locked={!isAdmin} onPress={() => openConfigRoute('simbolos')} />
                 <ConfigGroupRow icon="🗂" label="Categorias"      locked={!isAdmin} onPress={() => openConfigRoute('categorias')} />
                 <ConfigGroupRow icon="📅" label="Rotina do dia"   locked={!isAdmin} onPress={() => openConfigRoute('rotina')} />
                 <ConfigGroupRow icon="🏠" label="Cenas visuais"   locked={!isAdmin} onPress={() => openConfigRoute('cenas')} />
               </View>
             </View>
             {/* Grupo CUIDADOR — gated; opcoes mudam por estado */}
             <View style={styles.sGroupBlock}>
               <Text style={styles.sGroupTitle}>CUIDADOR</Text>
               <View style={styles.sCard}>
                 <ConfigGroupRow icon="🔐" label={isAdmin ? 'Senha' : (needsAdminSetup ? 'Criar senha' : 'Entrar como cuidador')} onPress={() => openConfigRoute('senha')} first />
                 <ConfigGroupRow icon="🔑" label="Chave da IA" locked={!isAdmin} onPress={() => openConfigRoute('chave-ia')} />
                 {isAdmin ? (
                   <ConfigGroupRow icon="🚪" label="Sair do modo cuidador" danger onPress={() => { handleAdminLogout(); resetConfigFields(); }} />
                 ) : null}
               </View>
             </View>
           </ScrollView>
         ) : (
           /* TODO Task 2b: substituir por wrapper ConfigDetailScreen com header
              "‹ Ajustes" + titulo + ScrollView contendo os sub-corpos por route. */
           <View />
         )}
       </View>
       ```
       Apos esta task, abrir Ajustes mostra a HOME agrupada corretamente; tocar
       qualquer linha (que nao seja 'home' ou tap em "Sair") muda `configRoute`
       e renderiza tela vazia — isso e ESPERADO ate a Task 2b completar.
    5. NAO ajustar `<IOSBottomSheet title="Ajustes" leftAction={...}>` — manter como
       esta.
  </action>
  <verify>
    <automated>cd "C:/Users/eduau/OneDrive/Área de Trabalho/fala" && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "function ConfigGroupRow" src/App.tsx` retorna 1.
    - `grep -n "sGroupTitle:\|sCard:\|sRow:\|sIcon:\|sChevron:\|sLockBadge:" src/App.tsx` retorna >= 6 (definicoes de style key).
    - `grep -n "configRoute === 'home'" src/App.tsx` retorna >= 1.
    - `grep -n "ConfigGroupRow" src/App.tsx` retorna >= 11 (definicao + 10 usos: 3 APP + 6 CONTEUDO + 2 CUIDADOR + 1 condicional Sair).
    - `grep -n "ConfigNavItem" src/App.tsx` retorna 0 (limpeza confirmada da Task 1).
    - `grep "fontFamily: '" src/App.tsx` retorna 0 (estilos novos usam `theme.typography.*`).
    - `grep -n "configSubheader" src/App.tsx` — apenas a definicao de style permanece;
      o JSX que a consumia dentro do sheet foi removido.
    - `npm run lint` passa sem erros novos.
  </acceptance_criteria>
  <done>
    Sheet "Ajustes" abre na home agrupada (3 grupos x cards x linhas com icon-chip +
    chevron + cadeado quando gated). Toques nas linhas mudam `configRoute` mas
    renderizam tela vazia ate a Task 2b — comportamento intermediario aceitavel.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2b: Wrapper de detalhe + render dos sub-corpos por route (split senha vs chave-ia)</name>
  <files>src/App.tsx</files>
  <read_first>
    - src/App.tsx (resultado da Task 2a — bloco IOSBottomSheet com HOME implementada e ramo else vazio)
    - src/App.tsx bloco do antigo `configSection === 'seguranca'` (linhas 3631-3741 originais) — sera dividido em 'senha' e 'chave-ia'
    - src/App.tsx blocos legados de cada configSection (voz, acessibilidade, perfil->aparencia, vocabulario, frases, simbolos, categorias, rotina, cenas)
    - src/theme.ts (theme.typography.title3 e theme.spacing.*)
  </read_first>
  <action>
    1. Adicionar em `makeStyles(theme)` os estilos do wrapper de detalhe:
       ```ts
       configDetailHeader: {
         flexDirection: 'row',
         alignItems: 'center',
         paddingHorizontal: theme.spacing.lg,
         paddingTop: theme.spacing.sm,
         paddingBottom: theme.spacing.md,
         gap: theme.spacing.sm
       },
       configBackButton: {
         flexDirection: 'row',
         alignItems: 'center',
         paddingVertical: theme.spacing.xs,
         paddingRight: theme.spacing.sm
       },
       configBackText: { ...theme.typography.body, color: theme.colors.primary },
       configDetailTitle: { ...theme.typography.title3, color: theme.colors.text, flex: 1, textAlign: 'center', marginRight: 60 },
       configDetailBody: { paddingHorizontal: theme.spacing.lg, paddingBottom: theme.spacing.xxl, gap: theme.spacing.md }
       ```
    2. Definir, no escopo de modulo (perto de ConfigGroupRow), o mapa de titulos:
       ```ts
       const CONFIG_ROUTE_TITLES: Record<Exclude<ConfigRoute, 'home'>, string> = {
         voz: 'Voz',
         acessibilidade: 'Acessibilidade',
         aparencia: 'Aparência',
         vocabulario: 'Vocabulário core',
         frases: 'Frases prontas',
         simbolos: 'Símbolos pessoais',
         categorias: 'Categorias',
         rotina: 'Rotina do dia',
         cenas: 'Cenas visuais',
         senha: 'Senha do cuidador',
         'chave-ia': 'Chave da IA'
       };
       ```
    3. Substituir o ramo `else` (TODO da Task 2a) por:
       ```tsx
       <View style={styles.configSheet}>
         <View style={styles.configDetailHeader}>
           <Pressable
             onPress={() => setConfigRoute('home')}
             style={styles.configBackButton}
             accessibilityRole="button"
             accessibilityLabel="Voltar para Ajustes"
           >
             <Text style={styles.configBackText}>‹ Ajustes</Text>
           </Pressable>
           <Text style={styles.configDetailTitle} numberOfLines={1}>
             {CONFIG_ROUTE_TITLES[configRoute]}
           </Text>
         </View>
         <ScrollView
           ref={configScrollRef}
           showsVerticalScrollIndicator={false}
           keyboardShouldPersistTaps="handled"
           contentContainerStyle={styles.configDetailBody}
         >
           {configRoute === 'voz' && (/* JSX existente do bloco voz, verbatim */)}
           {configRoute === 'acessibilidade' && (/* JSX existente do bloco acessibilidade, verbatim */)}
           {configRoute === 'aparencia' && (/* JSX existente do antigo bloco PERFIL, verbatim */)}
           {configRoute === 'vocabulario' && (/* JSX existente, verbatim */)}
           {configRoute === 'frases' && (/* JSX existente, verbatim */)}
           {configRoute === 'simbolos' && (/* JSX existente, verbatim */)}
           {configRoute === 'categorias' && (/* JSX existente, verbatim */)}
           {configRoute === 'rotina' && (/* JSX existente, verbatim */)}
           {configRoute === 'cenas' && (/* JSX existente, verbatim */)}
           {configRoute === 'senha' && (/* ver passo 4 */)}
           {configRoute === 'chave-ia' && (/* ver passo 5 */)}
         </ScrollView>
       </View>
       ```
    4. Bloco `configRoute === 'senha'`: COPIAR o JSX inteiro do antigo
       `configSection === 'seguranca'` (linhas 3631-3741), MAS dentro do branch
       `isAdmin` REMOVER apenas o sub-bloco "Chave da IA" (linhas 3722-3737); esse
       sub-bloco vai inteirinho para `configRoute === 'chave-ia'`. O restante
       (setup / login / trocar senha / Sair) permanece VERBATIM. NAO alterar
       handlers (`handleSaveAdminPassword`, `handleAdminLogin`,
       `handleUpdateAdminPassword`, `handleAdminLogout`, `hashSecret`,
       `adminPasswordHash`). Confirmar que os 3 handlers de auth continuam
       chamando `setIsConfigModalOpen(false)` em sucesso — esse comportamento
       legado e desejado e nao deve ser tocado.
    5. Bloco `configRoute === 'chave-ia'`: extrair o sub-bloco da Chave da IA
       (linhas 3722-3737) num wrapper igual ao das outras secoes
       (`<View style={[styles.configSectionCard, isHighContrast && styles.configSectionCardHighContrast]}>`).
       Se `!isAdmin`, mostrar somente um `<Text>` hint:
       `"Entre como cuidador para alterar a chave da IA."`.
    6. Verificar que `configRoute !== 'home'` cobre TODAS as 11 rotas do tipo
       ConfigRoute (cada uma com seu render `{configRoute === 'X' && (...)}`).
       Nenhum branch pode faltar.
    7. Quando `configRoute !== 'home'`, o usuario tem DOIS caminhos para sair: o
       "Fechar" do sheet (fecha modal) e o "‹ Ajustes" do detalhe (volta para a
       home dentro do sheet). Ambos sao corretos e nao conflitam.
  </action>
  <verify>
    <automated>cd "C:/Users/eduau/OneDrive/Área de Trabalho/fala" && npm run lint &amp;&amp; npm run test -- --runInBand</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "CONFIG_ROUTE_TITLES" src/App.tsx` retorna >= 2 (definicao + uso).
    - `grep -n "‹ Ajustes" src/App.tsx` retorna >= 1.
    - `grep -n "configRoute === 'senha'" src/App.tsx` retorna 1.
    - `grep -n "configRoute === 'chave-ia'" src/App.tsx` retorna 1.
    - `grep -n "configRoute === 'voz'\|configRoute === 'acessibilidade'\|configRoute === 'aparencia'\|configRoute === 'vocabulario'\|configRoute === 'frases'\|configRoute === 'simbolos'\|configRoute === 'categorias'\|configRoute === 'rotina'\|configRoute === 'cenas'" src/App.tsx` retorna 9 (uma para cada rota).
    - `grep -c "handleSaveAdminPassword\|handleAdminLogin\|handleUpdateAdminPassword" src/App.tsx` continua >= 6 (3 definicoes + 3 chamadas no JSX).
    - `grep "fontFamily: '" src/App.tsx` retorna 0.
    - `npm run lint` passa; `npm run test` 22/26 (4 pre-existentes herdados).
  </acceptance_criteria>
  <done>
    Drill-down completo: cada rota renderiza seu JSX legado verbatim dentro do
    wrapper de detalhe com header "‹ Ajustes  +  titulo". Chave da IA virou
    sub-tela propria; Senha mantem 3 estados (setup/entrar/trocar). Auth flow
    intacto (handlers ainda fecham o sheet em sucesso). Lint limpo, testes
    nao regridem.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Checkpoint humano — shell agrupado, drill-down e gating no device</name>
  <what-built>
    Shell de Ajustes agora abre numa home agrupada em 3 grupos (App / Conteudo da
    crianca / Cuidador) substituindo as 10 abas planas; cada linha abre uma
    sub-tela com "‹ Ajustes" no topo; grupos gated mostram 🔒 e levam ao gate de
    senha quando o cuidador esta deslogado. Os handlers de auth (login / criar
    senha / trocar senha) continuam fechando o sheet em sucesso — comportamento
    legado preservado verbatim. JSX dos editores nas sub-telas ainda e o legado
    verbatim — o restyling visual dentro de cada sub-tela vem nos planos 24-02 /
    24-03 / 24-04.
  </what-built>
  <how-to-verify>
    1. `npm run start` e abrir no iOS/Android.
    2. Tela principal -> tocar no botao "Cuidador" do header -> sheet de Ajustes
       deve abrir na HOME agrupada (3 grupos, NAO 10 abas).
    3. Com o cuidador DESLOGADO (estado padrao se a senha ainda nao existe ou
       depois de "Sair do modo cuidador"):
       - Grupo "APP": linhas Voz / Acessibilidade / Aparência sem cadeado, todas
         clicaveis.
       - Grupo "CONTEÚDO DA CRIANÇA": as 6 linhas mostram 🔒 a direita; tocar
         qualquer uma leva para a sub-tela "Senha do cuidador" (gate).
       - Grupo "CUIDADOR": linha "Criar senha" / "Entrar como cuidador" (depende
         de needsAdminSetup) leva ao gate; "Chave da IA" mostra 🔒.
       - NAO existe linha "Sair do modo cuidador" quando deslogado.
    4. Criar senha ou logar. COMPORTAMENTO ESPERADO (legado preservado verbatim):
       apos sucesso o sheet FECHA — esse e o comportamento dos handlers
       `handleSaveAdminPassword` / `handleAdminLogin` (chamam
       `setIsConfigModalOpen(false)`). Reabrir Ajustes tocando "Cuidador" no header
       deve mostrar a HOME desbloqueada (sem cadeados nos grupos "Conteúdo da
       criança" e "Cuidador", e com a linha "Sair do modo cuidador" em
       terracota/danger no grupo Cuidador).
    5. Drill-down: tocar qualquer linha (ex.: "Vocabulário core") -> abre sub-tela
       com header "‹ Ajustes" + titulo "Vocabulário core" + corpo do editor.
       Tocar "‹ Ajustes" -> volta para a home (sheet continua aberto).
    6. Fechar o sheet ("Fechar" top-left) e reabrir -> volta para a home (nao
       persiste na ultima sub-tela; openConfigModal apenas chama resetConfigFields
       que ja garante configRoute === 'home').
    7. Trocar senha em "Cuidador -> Senha": preencher os 3 campos e tocar
       "Atualizar senha". COMPORTAMENTO ESPERADO (legado preservado verbatim):
       apos sucesso o sheet FECHA (handler `handleUpdateAdminPassword` chama
       `setIsConfigModalOpen(false)`). Reabrir Ajustes mostra a HOME desbloqueada
       (`isAdmin === true`, sem cadeados).
    8. Editores ainda funcionam (smoke test):
       - Vocabulario core: reordenar uma palavra, adicionar, remover.
       - Frases prontas: adicionar uma frase, editar, remover.
       - Simbolos: abrir o draft a partir da camera/galeria (apenas confirmar que
         a tela abre — captura real e opcional).
       - Voz: tocar "Testar voz".
       - Aparencia: trocar Compacto/Padrao/Confortavel e a quantidade de colunas.
       - Acessibilidade: trocar tema (Salvia / Terracota / Sereno Escuro).
       - Senha: trocar senha (input "Senha atual" + "Nova" + "Confirmar" + botao);
         sheet deve fechar em sucesso (comportamento legado).
       - Chave da IA: editar e salvar.
    9. Visual: a home agrupada usa cards creme com hairlines internas, icon-chip
       em sálvia-soft (38x38), chevron `›` discreto a direita, header de grupo em
       UPPERCASE mudo. Linha "Sair" aparece em terracota/danger.
  </how-to-verify>
  <resume-signal>
    Responda "approved" ou descreva problemas (ex.: cadeado nao aparece, drill-down
    nao volta, editor X quebrou, o sheet NAO fecha apos criar senha/login/trocar
    senha — isso seria regressao do comportamento legado; ou ao reabrir Ajustes a
    HOME nao mostra a desbloqueada como esperado). Bugs visuais especificos das
    sub-telas — ex.: lista densa de vocabulario com 3 botoes ainda apertada —
    sao ESPERADOS aqui e serao consertados nos planos 24-02/03/04.
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| App rendering -> auth state | Linhas dos grupos exibem 🔒 com base em `isAdmin`; tap em linha gated chama `openConfigRoute` que redireciona para a sub-tela de senha. Nenhuma rota gated renderiza conteudo sensivel sem `isAdmin === true`. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-01 | E (Elevation of Privilege) | `openConfigRoute` | mitigate | Helper centraliza a checagem `!isAdmin && !APP_ROUTES.includes(route)` antes de mudar `configRoute`. Sub-telas protegidas tambem continuam a fazer o seu proprio gate via `!isAdmin ? <hint> : <editor>` legado (defesa em camadas). |
| T-24-02 | I (Information Disclosure) | render condicional `configRoute === 'X' && ...` | mitigate | Conteudo protegido nao aparece no DOM enquanto o gate nao deixa `configRoute` mudar; cadeado e a unica affordance visivel. |
| T-24-03 | T (Tampering) | persistencia | accept | Sem nova superficie de ataque — fluxo de auth (`hashSecret`, `adminPasswordHash`, `setAdminPassword`) preservado verbatim; apenas o involucro visual e a navegacao mudam. Sem novas chamadas de rede, persistencia ou input handling. |
</threat_model>

<verification>
- `npm run lint` (tsc --noEmit) limpo.
- `npm run test -- --runInBand` mantem 22/26 (4 pre-existentes herdados).
- Greps:
  - `grep "fontFamily: '" src/App.tsx` retorna 0 (VIS-02 segue fechado).
  - `grep -n "ConfigSection\|configSection\|setConfigSection\|ConfigNavItem" src/App.tsx` retorna 0.
  - `grep -n "configRoute\|ConfigRoute" src/App.tsx` retorna >= 12.
  - `grep -n "function ConfigGroupRow\|CONFIG_ROUTE_TITLES" src/App.tsx` retorna >= 3.
  - `grep -n "setConfigSection('seguranca')" src/App.tsx` retorna 0 (removido de openConfigModal).
  - Handlers de auth (handleAdminLogin, handleSaveAdminPassword, handleUpdateAdminPassword)
    continuam chamando setIsConfigModalOpen(false) — verbatim preservado.
- Checkpoint humano (Task 3) aprovado.
</verification>

<success_criteria>
- Sheet "Ajustes" abre na home agrupada de 3 grupos (CFG-01 parcial — visivel desde este plano; CFG-01 final fecha quando os planos 24-02/03/04 restilizarem as sub-telas).
- Toda linha leva a uma sub-tela dedicada com botao voltar (CFG-02 estruturalmente concluido — o restyling das sub-telas com tokens novos vem nos planos seguintes).
- Cuidador deslogado ve cadeados em "Conteudo da crianca" e "Cuidador", e tap em linha gated leva ao gate de senha (CFG-03 concluido funcionalmente; tokens de visual restantes nos planos seguintes).
- Auth flow preservado verbatim (`hashSecret`, `adminPasswordHash`, `setAdminPassword`, `needsAdminSetup`, `handleAdminLogin`, `handleSaveAdminPassword`, `handleUpdateAdminPassword`, `handleAdminLogout`) — INCLUSIVE o comportamento de fechar o sheet em sucesso.
- Sem azul iOS introduzido nos estilos novos; tokens de tema usados.
</success_criteria>

<output>
Apos completar, criar `.planning/phases/24-configuracoes-agrupadas-com-drill-down/24-01-SUMMARY.md`
seguindo o template padrao.
</output>
</content>
</invoke>