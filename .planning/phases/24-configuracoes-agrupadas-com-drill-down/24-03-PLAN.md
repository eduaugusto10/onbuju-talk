---
phase: 24-configuracoes-agrupadas-com-drill-down
plan: 03
type: execute
wave: 3
depends_on: [24-01, 24-02]
files_modified:
  - src/App.tsx
autonomous: true
requirements: [CFG-01, CFG-02]
tags: [config, drill-down, conteudo-crianca, vocabulario, frases, simbolos, categorias, rotina, cenas]

must_haves:
  truths:
    - "Cada item das sub-telas Vocabulário, Frases e Categorias ganha sua propria linha respiravel (sem 3 botoes minusculos espremidos)."
    - "A sub-tela Simbolos mantem os botoes Camera / Galeria e o draft existente, mas com o novo padrao de cards de drill-down."
    - "A sub-tela Simbolos preserva EXATAMENTE os 3 handlers reais de cada linha da galeria: openAudioRecorderFor (🎙 sempre), clearSymbolAudio (🔇 apenas quando hasAudio === true), removePersonalSymbol (✕ danger). NAO existe handler de 'editar (lápis)' — proibido inventar."
    - "Os botoes Camera/Galeria mantem disabled={pickerBusy} + style com opacity 0.6 quando pickerBusy, para impedir duplo-toque durante captura."
    - "A sub-tela Rotina mantem todo o editor de passos com dias, sem regressao."
    - "A sub-tela Cenas mantem o gerenciamento de cenas e edicao, sem regressao."
    - "Quando o cuidador esta deslogado e abre uma sub-tela do grupo, ve um hint pt-BR e nenhum editor — o gating do plano 24-01 ja redireciona para a tela de senha, entao este caso e defesa-em-camadas."
  artifacts:
    - path: "src/App.tsx"
      provides: "Sub-telas Vocabulario / Frases / Simbolos / Categorias / Rotina / Cenas restilizadas dentro do drill-down do sheet de Ajustes."
      contains: "configRoute === 'vocabulario'"
    - path: "src/App.tsx"
      provides: "Padrao de linha de item com leading label + trailing icon-buttons em chip arredondado (sem o icone-quadrado minusculo legado)."
      contains: "drillItemRow"
  key_links:
    - from: "Sub-telas do grupo"
      to: "editores existentes"
      via: "addCoreVocabularyWord / moveCoreVocabularyWord / removeCoreVocabularyWord / resetCoreVocabulary / addSavedPhrase / removeSavedPhrase / startEditingPhrase / commitEditingPhrase / cancelEditingPhrase / clearPhraseHistory / pickFromCamera / pickFromGallery / openAudioRecorderFor / clearSymbolAudio / removePersonalSymbol / addCustomCategory / removeCustomCategory / startEditCategory / commitEditCategory / cancelEditCategory / rotina e cenas verbatim"
      pattern: "addCoreVocabularyWord|addSavedPhrase|openAudioRecorderFor|clearSymbolAudio|removePersonalSymbol|pickFromCamera|addCustomCategory|routineSteps|visualScenes"
---

<objective>
Restilizar as 6 sub-telas do grupo "Conteúdo da criança" dentro do drill-down
criado no plano 24-01: Vocabulário core, Frases prontas, Símbolos pessoais,
Categorias, Rotina do dia e Cenas visuais. Eliminar listas densas (rótulo + 3
botões minúsculos espremidos) onde possível usando o novo padrão de linha
espaçada (`drillItemRow`), e tokenizar todos os textos consumindo `theme.*`.

A LOGICA dos editores e preservada VERBATIM (vocabulario core add/remove/reorder/reset;
frases edit/delete/clear-history; simbolos pessoais camera/galeria/draft/audio
incluindo openAudioRecorderFor + clearSymbolAudio + removePersonalSymbol;
categorias CRUD; rotina passos com dias; cenas com hotspots). Apenas o invólucro
visual muda. Componentes auxiliares atuais (SymbolDraft, RoutineDraft,
SceneEditor, SceneViewer) e seus handlers continuam intactos.

Purpose: avancar CFG-01 (home agrupada) e fechar CFG-02 (drill-down sem listas
densas) para o grupo "Conteudo da crianca". Esse grupo concentra a maior dor de
densidade do antigo design — 5 dos 6 itens tinham listas com 3 botoes
amontoados.
Output: 6 sub-telas restilizadas, funcionalmente identicas, com cabecalhos de
secao UPPERCASE, cards Salvia & Creme e linhas de item com respiro.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/24-configuracoes-agrupadas-com-drill-down/24-CONTEXT.md
@.planning/phases/24-configuracoes-agrupadas-com-drill-down/24-01-PLAN.md
@.planning/phases/24-configuracoes-agrupadas-com-drill-down/24-02-PLAN.md
@.claude/skills/sketch-findings-fala/references/configuracoes.md
@.claude/skills/sketch-findings-fala/references/fundamentos-visuais.md
@src/theme.ts
@src/App.tsx

<interfaces>
<!-- Contratos a consumir VERBATIM. -->

Editores e estado (NAO redesenhar / NAO alterar):
```ts
// Vocabulario core
const addCoreVocabularyWord = useCallback(...);
const removeCoreVocabularyWord = useCallback((word: string) => ...);
const moveCoreVocabularyWord = useCallback((word: string, direction: -1 | 1) => ...);
const resetCoreVocabulary = useCallback(...);
const CORE_VOCABULARY_MAX: number;
const newCoreWord: string;
const setNewCoreWord: (v: string) => void;

// Frases prontas e historico
const addSavedPhrase = useCallback(...);            // adiciona nova frase
const removeSavedPhrase = (id: string) => ...;
const startEditingPhrase = (phrase: ...) => ...;
const commitEditingPhrase = useCallback(...);
const cancelEditingPhrase = useCallback(...);
const clearPhraseHistory = useCallback(...);
const SAVED_PHRASES_MAX: number;
const PHRASE_HISTORY_MAX: number;
const editingPhraseId: string | null;
const editingPhraseText: string;
const setEditingPhraseText: (t: string) => void;
// Estado do input "nova frase" (NAO criar novo estado — usar o existente):
// CONFIRMADO em src/App.tsx linha 387:
//   const [newPhraseInput, setNewPhraseInput] = useState('');
// Usado em linha 918 (normalizeSpokenText(newPhraseInput)), linha 937
// (setNewPhraseInput('')), e no TextInput de "nova frase" em linhas 2874-2876:
//   <TextInput value={newPhraseInput} onChangeText={setNewPhraseInput} ... />
const newPhraseInput: string;
const setNewPhraseInput: (v: string) => void;

// Simbolos pessoais — EXATAMENTE 3 handlers reais por linha da galeria.
//   NAO EXISTE handler de "editar (lápis)" para simbolo pessoal. NAO inventar.
//   O label do simbolo so e definido no draft. Para alterar label de simbolo
//   salvo, o usuario apaga (✕) e recria — comportamento legado preservado.
const pickFromCamera = useCallback(async () => ...);            // ja respeita pickerBusy internamente
const pickFromGallery = useCallback(async () => ...);           // ja respeita pickerBusy internamente
const pickerBusy: boolean;                                       // flag de captura em andamento
const openAudioRecorderFor = useCallback(async (id: string) => ...);  // 🎙 — grava/regrava voz do simbolo (SEMPRE visivel)
const clearSymbolAudio = useCallback(async (id: string) => ...);      // 🔇 — remove voz do simbolo (so renderizar quando hasAudio === true)
const removePersonalSymbol = useCallback((id: string) => ...);        // ✕ — exclui simbolo da galeria (danger, SEMPRE visivel)
// + draft existente (symbolDraftCard) — preservar verbatim, apenas alinhar paddings/cards

// Categorias customizadas
const addCustomCategory = useCallback(...);
const removeCustomCategory = (id: string) => ...;
const startEditCategory = (cat: ...) => ...;
const commitEditCategory = useCallback(...);
const cancelEditCategory = useCallback(...);
const customCategories: Category[];
const editingCategoryId: string | null;
const editingCategoryName: string;
const setEditingCategoryName: (n: string) => void;

// Rotina e cenas — usar o JSX completo legado, apenas envolver no novo padrao
```

Tokens novos a adicionar nesta passagem (em `makeStyles(theme)`):
- `drillItemRow` — linha respiravel: flex row, padding generoso, gap, alinhamento centro.
- `drillItemRowDivider` — hairline interna entre itens.
- `drillItemLabel` — label do item (flex:1, numberOfLines:1, body).
- `drillItemSubLabel` — sub-label opcional (footnote muted) — ex.: "3 simbolos" na categoria.
- `drillIconButton` — chip 32x32 arredondado neutro (para ↑ ↓ 🎙 🔇).
- `drillIconButtonDanger` — variante danger (para ✕).
- `drillIconButtonText` — body em primaryInk.
- `drillIconButtonTextDanger` — body em danger.
- `drillInlineInput` — TextInput inline (substitui `phraseEditorInput`).
- `drillAddRow` — row para "novo item + botao Adicionar" com gap.
- `drillEmptyHint` — texto centralizado para listas vazias.

Tokens existentes (do plano 24-02) ja disponiveis e reusados:
- `drillSectionTitle`, `drillSectionCard`, `drillFieldHint`, `drillPrimaryButton`,
  `drillSecondaryButton`, `drillDangerButton`.

Anti-padrao a evitar (skill `configuracoes.md`):
- Lista "rotulo + 3 botoezinhos espremidos" — esta sendo eliminada AQUI. Os
  botoes (↑/↓/✎/✕) continuam mas em chips 32x32 com gap visivel.

Anti-padrao especifico para simbolos pessoais:
- PROIBIDO inventar um botao "editar (lapis)" — nao existe handler. A galeria
  de simbolos pessoais tem EXATAMENTE 3 acoes reais por linha:
  - 🎙 openAudioRecorderFor — SEMPRE visivel
  - 🔇 clearSymbolAudio — SO renderizado quando hasAudio === true
  - ✕ removePersonalSymbol — SEMPRE visivel, variante danger
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Adicionar tokens de item-row + restilizar 'Vocabulário core' e 'Frases prontas'</name>
  <files>src/App.tsx</files>
  <read_first>
    - src/App.tsx bloco `configRoute === 'vocabulario'` (do 24-01, era 'vocabulario' linhas 2691-2784 originais)
    - src/App.tsx bloco `configRoute === 'frases'` (do 24-01, era linhas 2786-2907 originais)
    - src/App.tsx linha 387 — DECLARACAO do estado do TextInput de "nova frase":
        `const [newPhraseInput, setNewPhraseInput] = useState('');`
      Usos confirmados: linha 918 (`normalizeSpokenText(newPhraseInput)`),
      linha 937 (`setNewPhraseInput('')`), linhas 2874-2876 (TextInput value/onChangeText).
      REUSAR este estado — NAO criar nenhum novo `newPhraseInput` / `setNewPhraseInput`.
    - src/App.tsx handlers addCoreVocabularyWord/removeCoreVocabularyWord/moveCoreVocabularyWord/resetCoreVocabulary (linhas ~844-880)
    - src/App.tsx handlers de frases (addSavedPhrase, removeSavedPhrase, startEditingPhrase, commitEditingPhrase, cancelEditingPhrase, clearPhraseHistory)
    - .claude/skills/sketch-findings-fala/references/configuracoes.md (Listas densas eliminadas)
  </read_first>
  <action>
    1. Adicionar em `makeStyles(theme)` os tokens novos descritos em `<interfaces>`:
       ```ts
       drillItemRow: {
         flexDirection: 'row',
         alignItems: 'center',
         gap: theme.spacing.sm,
         paddingVertical: theme.spacing.md,
         paddingHorizontal: theme.spacing.lg
       },
       drillItemRowDivider: {
         borderTopWidth: StyleSheet.hairlineWidth,
         borderTopColor: theme.colors.border
       },
       drillItemLabel: {
         flex: 1,
         ...theme.typography.body,
         color: theme.colors.text
       },
       drillItemSubLabel: {
         ...theme.typography.footnote,
         color: theme.colors.textMuted
       },
       drillIconButton: {
         width: 32, height: 32, borderRadius: theme.radii.full,
         alignItems: 'center', justifyContent: 'center',
         backgroundColor: theme.colors.primarySoft
       },
       drillIconButtonDanger: { backgroundColor: theme.colors.dangerSoft },
       drillIconButtonText: { ...theme.typography.body, color: theme.colors.primaryInk },
       drillIconButtonTextDanger: { color: theme.colors.danger },
       drillInlineInput: {
         flex: 1,
         ...theme.typography.body,
         color: theme.colors.text,
         backgroundColor: theme.colors.surface2,
         borderRadius: theme.radii.md,
         borderWidth: 1,
         borderColor: theme.colors.border,
         paddingVertical: theme.spacing.sm,
         paddingHorizontal: theme.spacing.md
       },
       drillAddRow: {
         flexDirection: 'row',
         alignItems: 'center',
         gap: theme.spacing.sm,
         paddingHorizontal: theme.spacing.lg,
         paddingVertical: theme.spacing.md
       },
       drillEmptyHint: {
         ...theme.typography.body,
         color: theme.colors.textMuted,
         textAlign: 'center',
         paddingVertical: theme.spacing.lg
       },
       drillListCard: {
         backgroundColor: theme.colors.surface,
         borderRadius: theme.radii.lg,
         borderWidth: 1,
         borderColor: theme.colors.border,
         overflow: 'hidden',
         ...theme.shadows.sm
       }
       ```

    2. Substituir o JSX do bloco `configRoute === 'vocabulario'` por (preservando
       handlers verbatim):
       ```tsx
       {configRoute === 'vocabulario' && (
         !isAdmin ? (
           <Text style={styles.drillEmptyHint}>Entre como cuidador para editar o vocabulário core.</Text>
         ) : (
           <>
             <Text style={styles.drillSectionTitle}>PALAVRAS ATUAIS</Text>
             <View style={styles.drillListCard}>
               {coreVocabulary.length === 0 ? (
                 <Text style={styles.drillEmptyHint}>Nenhuma palavra no core ainda.</Text>
               ) : (
                 coreVocabulary.map((word, index) => (
                   <View
                     key={`core-edit-${word}`}
                     style={[styles.drillItemRow, index > 0 && styles.drillItemRowDivider]}
                   >
                     <Text style={styles.drillItemLabel} numberOfLines={1}>{word.toUpperCase()}</Text>
                     <Pressable
                       onPress={() => moveCoreVocabularyWord(word, -1)}
                       disabled={index === 0}
                       style={[styles.drillIconButton, index === 0 && { opacity: 0.4 }]}
                       accessibilityRole="button"
                       accessibilityLabel={`Mover ${word} para cima`}
                     >
                       <Text style={styles.drillIconButtonText}>↑</Text>
                     </Pressable>
                     <Pressable
                       onPress={() => moveCoreVocabularyWord(word, 1)}
                       disabled={index === coreVocabulary.length - 1}
                       style={[styles.drillIconButton, index === coreVocabulary.length - 1 && { opacity: 0.4 }]}
                       accessibilityRole="button"
                       accessibilityLabel={`Mover ${word} para baixo`}
                     >
                       <Text style={styles.drillIconButtonText}>↓</Text>
                     </Pressable>
                     <Pressable
                       onPress={() => removeCoreVocabularyWord(word)}
                       style={[styles.drillIconButton, styles.drillIconButtonDanger]}
                       accessibilityRole="button"
                       accessibilityLabel={`Remover ${word}`}
                     >
                       <Text style={[styles.drillIconButtonText, styles.drillIconButtonTextDanger]}>✕</Text>
                     </Pressable>
                   </View>
                 ))
               )}
             </View>

             <Text style={styles.drillSectionTitle}>ADICIONAR</Text>
             <View style={styles.drillSectionCard}>
               <View style={{ flexDirection: 'row', gap: theme.spacing.sm }}>
                 <TextInput
                   value={newCoreWord}
                   onChangeText={setNewCoreWord}
                   placeholder="Nova palavra (ex.: água)"
                   placeholderTextColor={theme.colors.textMuted}
                   autoCapitalize="none"
                   autoCorrect={false}
                   style={styles.drillInlineInput}
                   onSubmitEditing={addCoreVocabularyWord}
                 />
                 <Pressable
                   onPress={addCoreVocabularyWord}
                   style={styles.drillPrimaryButton}
                   accessibilityRole="button"
                   accessibilityLabel="Adicionar palavra ao core"
                 >
                   <Text style={styles.drillPrimaryButtonText}>Adicionar</Text>
                 </Pressable>
               </View>
               <Text style={styles.drillFieldHint}>
                 Máximo de {CORE_VOCABULARY_MAX} palavras. Duplicadas são ignoradas.
               </Text>
             </View>

             <Pressable
               onPress={resetCoreVocabulary}
               style={styles.drillSecondaryButton}
               accessibilityRole="button"
               accessibilityLabel="Restaurar vocabulário core padrão"
             >
               <Text style={styles.drillSecondaryButtonText}>Restaurar padrão</Text>
             </Pressable>
           </>
         )
       )}
       ```
    3. Substituir o JSX do bloco `configRoute === 'frases'` por uma estrutura
       analoga:
       - Card de lista (`drillListCard`) com `drillItemRow + drillItemRowDivider`,
         label da frase no `drillItemLabel`, botoes ✎/✕ em
         `drillIconButton`/`drillIconButtonDanger`.
       - Modo de edicao usa `drillInlineInput` + botoes OK/✕. Estado preservado
         verbatim: `editingPhraseId`, `editingPhraseText`, `setEditingPhraseText`,
         `commitEditingPhrase`, `cancelEditingPhrase`.
       - Em seguida, secao "ADICIONAR" com `drillInlineInput` ligado ao estado
         JA EXISTENTE `newPhraseInput` / `setNewPhraseInput` (declarado em
         src/App.tsx linha 387). NAO criar novo estado — usar verbatim a
         declaracao existente. O TextInput deve ser:
         ```tsx
         <TextInput
           value={newPhraseInput}
           onChangeText={setNewPhraseInput}
           placeholder="Nova frase (ex: quero agua)"
           placeholderTextColor={theme.colors.textMuted}
           style={styles.drillInlineInput}
           onSubmitEditing={addSavedPhrase}
         />
         ```
         Botao `drillPrimaryButton` ("Adicionar") chama `addSavedPhrase` verbatim.
       - Por fim, secao "HISTÓRICO" com um botao `drillSecondaryButton` rotulado
         `Limpar histórico ({phraseHistory.length})` chamando `clearPhraseHistory`.
       - Hint pt-BR no final:
         `Máximo de {SAVED_PHRASES_MAX} frases. Histórico guarda as últimas {PHRASE_HISTORY_MAX} faladas.`
       Preservar verbatim os handlers `addSavedPhrase`, `removeSavedPhrase`,
       `startEditingPhrase`, `commitEditingPhrase`, `cancelEditingPhrase`,
       `clearPhraseHistory`, e o estado `newPhraseInput` / `setNewPhraseInput`
       (linha 387 — NAO duplicar).
  </action>
  <verify>
    <automated>cd "C:/Users/eduau/OneDrive/Área de Trabalho/fala" && npm run lint &amp;&amp; npm run test -- --runInBand</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "drillItemRow:\|drillItemRowDivider:\|drillItemLabel:\|drillIconButton:\|drillIconButtonDanger:\|drillInlineInput:\|drillEmptyHint:\|drillListCard:" src/App.tsx` retorna >= 8.
    - Dentro de `configRoute === 'vocabulario'`: `moveCoreVocabularyWord`,
      `removeCoreVocabularyWord`, `addCoreVocabularyWord`, `resetCoreVocabulary`,
      `CORE_VOCABULARY_MAX`, `newCoreWord`, `setNewCoreWord` aparecem todos.
    - Dentro de `configRoute === 'frases'`: `addSavedPhrase`, `removeSavedPhrase`,
      `startEditingPhrase`, `commitEditingPhrase`, `cancelEditingPhrase`,
      `clearPhraseHistory`, `SAVED_PHRASES_MAX`, `PHRASE_HISTORY_MAX`,
      `newPhraseInput`, `setNewPhraseInput` aparecem todos.
    - `grep -c "const \[newPhraseInput" src/App.tsx` retorna 1 (declaracao
      unica em linha 387 — NAO foi duplicada).
    - Bloco vocabulario NAO contem `styles.coreVocabEditorList`,
      `styles.coreVocabEditorRow`, `styles.coreVocabEditorButton` (regressao
      visual — esses estilos legados ficam ate o plano de limpeza).
    - Bloco frases NAO contem `styles.phraseEditorList`,
      `styles.phraseEditorButton` (mesmo motivo).
    - `npm run lint` passa; `npm run test` 22/26.
  </acceptance_criteria>
  <done>Vocabulario e Frases restilizados com linhas respiraveis (chip 32x32 com gap), edicao inline funcional, hints pt-BR tokenizados, estado newPhraseInput (linha 387) reusado sem duplicacao.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Restilizar 'Símbolos pessoais' e 'Categorias' usando os novos tokens</name>
  <files>src/App.tsx</files>
  <read_first>
    - src/App.tsx bloco `configRoute === 'simbolos'` (era 'simbolos' linhas 2909-3073 originais) incluindo o sub-card de symbolDraft e a galeria com handlers openAudioRecorderFor / clearSymbolAudio / removePersonalSymbol (linhas 3020-3066)
    - src/App.tsx bloco `configRoute === 'categorias'` (era linhas 3074-3189 originais)
    - src/App.tsx handlers pickFromCamera (~linha 1056), pickFromGallery (~linha 1029), openAudioRecorderFor (~linha 1295), clearSymbolAudio (~linha 1321), removePersonalSymbol (~linha 1111), addCustomCategory, removeCustomCategory, startEditCategory, commitEditCategory, cancelEditCategory
    - src/App.tsx estado pickerBusy (linha 392) — flag de captura em andamento
  </read_first>
  <action>
    1. Bloco `configRoute === 'simbolos'`:
       - Cabecalho de secao `<Text style={styles.drillSectionTitle}>FONTES</Text>`.
       - `<View style={styles.drillSectionCard}>` com dois botoes
         `drillPrimaryButton` lado a lado (Camera, Galeria), gap consistente.
         AMBOS DEVEM TER `disabled={pickerBusy}` + style array com
         `pickerBusy && { opacity: 0.6 }` (preserva o comportamento legado de
         bloquear duplo-toque durante a captura — visivel nas linhas 2924-2925 e
         2933-2934 do bloco legado). Codigo EXATO:
         ```tsx
         <Pressable
           onPress={() => void pickFromCamera()}
           disabled={pickerBusy}
           style={[styles.drillPrimaryButton, pickerBusy && { opacity: 0.6 }]}
           accessibilityRole="button"
           accessibilityLabel="Tirar foto para novo símbolo"
         >
           <Text style={styles.drillPrimaryButtonText}>📷 Câmera</Text>
         </Pressable>
         <Pressable
           onPress={() => void pickFromGallery()}
           disabled={pickerBusy}
           style={[styles.drillPrimaryButton, pickerBusy && { opacity: 0.6 }]}
           accessibilityRole="button"
           accessibilityLabel="Escolher imagem da galeria"
         >
           <Text style={styles.drillPrimaryButtonText}>🖼 Galeria</Text>
         </Pressable>
         ```
         AMBOS os Pressables OBRIGATORIAMENTE com `disabled={pickerBusy}` e o
         style array com `pickerBusy && { opacity: 0.6 }`. Hint pt-BR abaixo
         dentro do mesmo `drillSectionCard`:
         "Tire uma foto familiar ou escolha da galeria. Você pode dar um rótulo
         e gravar a voz do cuidador para o novo símbolo."
       - Se `!isAdmin`: render `<Text style={styles.drillEmptyHint}>Entre como cuidador para gerenciar símbolos pessoais.</Text>` e SAIR.
       - Se o draft estiver ativo (`pendingSymbolImage` truthy): preservar
         EXATAMENTE o JSX legado do `symbolDraftCard` (audio recorder, naming
         etc.) — apenas trocar o wrapper externo de
         `[styles.symbolDraftCard, isHighContrast && styles.configSectionCardHighContrast]`
         para `styles.drillSectionCard`. Demais elementos internos do draft
         (botoes de gravacao etc.) NAO sao tocados aqui — sao componentes
         compartilhados que o plano de limpeza tratara depois.
       - Em seguida, secao "GALERIA DE SÍMBOLOS PESSOAIS" com `drillListCard`
         listando `personalSymbols` (logica existente). Cada linha usa
         `drillItemRow + drillItemRowDivider` (no primeiro item, sem divider) e
         estrutura:
         - Image thumb (mantem `styles.personalSymbolThumb` legado por ora).
         - View com label (`drillItemLabel`) + sub-label `drillItemSubLabel`
           contendo o nome da categoria (ou "Sem categoria").
         - EXATAMENTE 3 botoes de acao (mesmos do legado linhas 3039-3064),
           PRESERVADOS VERBATIM em comportamento — apenas trocando estilos
           legados (`styles.phraseEditorButton` / `styles.phraseEditorButtonDanger`)
           pelos novos `styles.drillIconButton` / `styles.drillIconButtonDanger`.
           A ORDEM e VISIBILIDADE sao OBRIGATORIAS:
           ```tsx
           {/* 1) 🎙 openAudioRecorderFor — Gravar/Regravar voz — SEMPRE visivel */}
           <Pressable
             onPress={() => void openAudioRecorderFor(symbol.id)}
             style={styles.drillIconButton}
             accessibilityRole="button"
             accessibilityLabel={hasAudio ? `Regravar voz do símbolo ${symbol.label}` : `Gravar voz para ${symbol.label}`}
           >
             <Text style={styles.drillIconButtonText}>🎙</Text>
           </Pressable>
           {/* 2) 🔇 clearSymbolAudio — Remover voz — SO renderizar quando hasAudio === true */}
           {hasAudio && (
             <Pressable
               onPress={() => void clearSymbolAudio(symbol.id)}
               style={styles.drillIconButton}
               accessibilityRole="button"
               accessibilityLabel={`Remover voz do símbolo ${symbol.label}`}
             >
               <Text style={styles.drillIconButtonText}>🔇</Text>
             </Pressable>
           )}
           {/* 3) ✕ removePersonalSymbol — Remover simbolo da galeria — sempre visivel, variante danger */}
           <Pressable
             onPress={() => removePersonalSymbol(symbol.id)}
             style={[styles.drillIconButton, styles.drillIconButtonDanger]}
             accessibilityRole="button"
             accessibilityLabel={`Remover símbolo ${symbol.label}`}
           >
             <Text style={[styles.drillIconButtonText, styles.drillIconButtonTextDanger]}>✕</Text>
           </Pressable>
           ```
         Manter o calculo de `hasAudio = !!symbol.audioUri` e o emoji indicador
         no label (`{symbol.label}{hasAudio ? ' 🔊' : ''}`) tal como no legado.
       - PROIBIDO: criar botao "editar (lapis)" — esse handler nao existe no
         codigo. A galeria de simbolos pessoais tem EXATAMENTE as 3 acoes
         listadas acima: 🎙 (openAudioRecorderFor), 🔇 condicional
         (clearSymbolAudio quando hasAudio), ✕ (removePersonalSymbol).
       - Preservar handlers e estado VERBATIM.
    2. Bloco `configRoute === 'categorias'`:
       - Secao "CATEGORIAS ATUAIS" -> `drillListCard` com `customCategories.map`.
         Cada linha: `drillItemRow` com `drillItemLabel` (cat.name) e abaixo um
         `drillItemSubLabel` ("3 simbolos"). Modo de edicao: input inline com
         `drillInlineInput` + botoes OK / ✕. Modo normal: botoes ✎ / ✕ em
         `drillIconButton`/danger.
       - Hint pt-BR ao final da secao: `Remover uma categoria não apaga os símbolos — eles ficam como "Sem categoria".`
       - Secao "ADICIONAR" -> `drillSectionCard` com row TextInput
         (`drillInlineInput`) + `drillPrimaryButton` "Adicionar". Handler
         `addCustomCategory` verbatim.
       - Empty state: `<Text style={styles.drillEmptyHint}>Nenhuma categoria customizada ainda.</Text>`.
       - Preservar todos os handlers VERBATIM.
    3. Em ambos os blocos, remover qualquer `highContrast={...}` e
       `isHighContrast && styles.X` introduzidos no JSX restilizado — confiar nos
       tokens de tema. (Excecao: o `symbolDraftCard` que mantemos verbatim
       continua com a propriedade isHighContrast — nao tocar.)
  </action>
  <verify>
    <automated>cd "C:/Users/eduau/OneDrive/Área de Trabalho/fala" && npm run lint &amp;&amp; npm run test -- --runInBand</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "configRoute === 'simbolos'" src/App.tsx` retorna 1.
    - `grep -n "configRoute === 'categorias'" src/App.tsx` retorna 1.
    - Bloco simbolos contem `pickFromCamera` e `pickFromGallery`, AMBOS com
      `disabled={pickerBusy}` E `pickerBusy && { opacity: 0.6 }` no style array.
      `grep -c "disabled={pickerBusy}" src/App.tsx` dentro do bloco simbolos >= 2.
    - Bloco simbolos contem `openAudioRecorderFor` (sempre), `clearSymbolAudio`
      dentro de um `{hasAudio && (...)}` (condicional), e `removePersonalSymbol`
      (sempre) na linha de cada simbolo da galeria. EXATAMENTE essas 3 acoes — nenhuma outra.
    - Bloco simbolos NAO contem string literal `lapis` nem `✎` nem `editar` na
      galeria (nao existe handler de edicao de simbolo pessoal — proibido inventar).
    - Bloco categorias contem `addCustomCategory`, `removeCustomCategory`,
      `startEditCategory`, `commitEditCategory`, `cancelEditCategory`.
    - Bloco categorias NAO contem `styles.phraseEditorList` /
      `styles.phraseEditorRow` / `styles.phraseEditorButton` /
      `styles.personalSymbolCategory` (regressao visual — o estilo
      `personalSymbolCategory` foi substituido por `drillItemSubLabel`).
    - `npm run lint` passa; `npm run test` 22/26.
  </acceptance_criteria>
  <done>Simbolos e Categorias restilizados; draft de simbolo preserva fluxo verbatim mas dentro do novo card; os 3 handlers reais (openAudioRecorderFor / clearSymbolAudio condicional / removePersonalSymbol) preservados na galeria sem invencao de "lapis"; Camera/Galeria mantem disabled={pickerBusy} + opacity 0.6.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Restilizar 'Rotina do dia' e 'Cenas visuais' (envolucros + cabecalhos; corpos editor verbatim)</name>
  <files>src/App.tsx</files>
  <read_first>
    - src/App.tsx bloco `configRoute === 'rotina'` (era linhas 3191-3453 originais)
    - src/App.tsx bloco `configRoute === 'cenas'` (era linhas 3454-3536 originais)
    - SceneEditor, SceneViewer (componentes externos referenciados ao final de src/App.tsx) — apenas verificar que continuam sendo invocados pelos mesmos triggers
  </read_first>
  <action>
    1. Bloco `configRoute === 'rotina'`: o editor de rotina e MUITO grande
       (passos com dias, draft de passo, dias da semana). Esta sub-tela NAO recebe
       restyling profundo nesta fase (esta no escopo dos Deferred Ideas em
       24-CONTEXT.md). Aplicar APENAS o invólucro do drill-down:
       - Se `!isAdmin`: `<Text style={styles.drillEmptyHint}>Entre como cuidador para editar a rotina do dia.</Text>`.
       - Senao: envolver todo o JSX legado em `<View style={styles.drillSectionCard}>` (um único card)
         para garantir surface tematizada e remover o array
         `[styles.configSectionCard, isHighContrast && styles.configSectionCardHighContrast]` do wrapper externo.
       - NAO mexer nos botoes / inputs / chips de dias internos. Eles continuam
         legacy — sao cobertos pelo proximo refactor (fase 25 ou Future).
       - Cabecalho de secao opcional: `<Text style={styles.drillSectionTitle}>PASSOS DO DIA</Text>` antes do card.
       - Hint pt-BR mantido se existir.
    2. Bloco `configRoute === 'cenas'`: mesmo tratamento.
       - Se `!isAdmin`: `<Text style={styles.drillEmptyHint}>Entre como cuidador para gerenciar cenas visuais.</Text>`.
       - Senao: cabecalho `<Text style={styles.drillSectionTitle}>CENAS</Text>` + envolver o
         JSX legado em `<View style={styles.drillSectionCard}>`.
       - Os triggers `editExistingScene`, `viewerScene`, `editingScene`
         continuam disparando os modais SceneEditor / SceneViewer ja existentes
         (renderizados FORA do sheet, no nivel da raiz do componente App.tsx) —
         NAO mover.
    3. Confirmar que nenhum dos dois blocos contem `styles.configSectionCard` ou
       `styles.configSectionCardHighContrast` no wrapper EXTERNO (substituidos
       por `styles.drillSectionCard`). Internos do editor podem manter estilos
       legados — sao escopo de outra fase.
  </action>
  <verify>
    <automated>cd "C:/Users/eduau/OneDrive/Área de Trabalho/fala" && npm run lint &amp;&amp; npm run test -- --runInBand</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "configRoute === 'rotina'" src/App.tsx` retorna 1.
    - `grep -n "configRoute === 'cenas'" src/App.tsx` retorna 1.
    - Em ambos os blocos, o WRAPPER externo nao contem mais
      `styles.configSectionCard`. (O JSX interno do editor de rotina pode ainda
      conter `styles.routineDraftCard` etc. — fora de escopo.)
    - `SceneEditor` e `SceneViewer` continuam sendo renderizados (existe o
      bloco `{viewerScene && <SceneViewer .../>}` e `{editingScene && <SceneEditor .../>}` fora do sheet).
    - `grep "fontFamily: '" src/App.tsx` retorna 0.
    - `npm run lint` passa; `npm run test` 22/26.
  </acceptance_criteria>
  <done>Rotina e Cenas ganham wrapper drill-down com card creme e gating defensivo; corpos do editor preservados verbatim para futuras passagens.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| App rendering -> isAdmin | Cada sub-tela do grupo "Conteudo da crianca" mantem o gate defensivo `!isAdmin ? <hint> : <editor>`, redundante mas explicito (defesa em camadas — o gating principal e em `openConfigRoute` do plano 24-01). |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-05 | I (Information Disclosure) | render Vocabulario/Frases/Simbolos/Categorias/Rotina/Cenas | mitigate | Cada sub-tela mantem o gate defensivo `!isAdmin ? <hint> : <editor>`; combinado com o redirect de `openConfigRoute` (24-01), nenhum editor renderiza sem `isAdmin === true`. |
| T-24-06 | E (Elevation of Privilege) | handlers de mutacao | accept | Sem nova superficie de ataque — handlers de mutacao (addCoreVocabularyWord, removeSavedPhrase, openAudioRecorderFor, clearSymbolAudio, removePersonalSymbol, removeCustomCategory etc.) sao preservados verbatim e ja levam em conta `isAdmin` por estarem render-gated; fluxo de auth (hashSecret, adminPasswordHash, setAdminPassword) preservado verbatim; apenas o invólucro visual e a navegacao mudam. Sem novas chamadas de rede, persistencia ou input handling. |
</threat_model>

<verification>
- `npm run lint` limpo; `npm run test` 22/26 (sem novas falhas).
- `grep "fontFamily: '" src/App.tsx` retorna 0 (VIS-02 segue fechado).
- As 6 sub-telas do grupo "Conteudo da crianca" estao no novo padrao drill-down
  (cabecalhos UPPERCASE, cards creme, linhas com gap). Listas densas (3
  botoezinhos minusculos) eliminadas em vocabulario / frases / categorias.
- Galeria de simbolos pessoais mantem EXATAMENTE os 3 handlers reais
  (openAudioRecorderFor sempre / clearSymbolAudio so quando hasAudio /
  removePersonalSymbol sempre) — nenhum botao "editar (lapis)" inventado.
- Camera/Galeria mantem `disabled={pickerBusy}` + `opacity: 0.6` quando
  pickerBusy — sem regressao de duplo-toque.
- Estado `newPhraseInput` (linha 387) reusado, NAO duplicado.
- Smoke test manual recomendado durante o plano 24-04 (checkpoint final).
</verification>

<success_criteria>
- CFG-02 efetivamente fecha para o grupo "Conteudo da crianca": cada item tem
  sua propria sub-tela e as listas densas foram eliminadas onde possivel.
- CFG-01 reforcado: a home agrupada do plano 24-01 agora encontra sub-telas
  visualmente coerentes ao acessar cada item do grupo "Conteudo da crianca".
- Logica preservada: 100% dos handlers e estados sao reusados verbatim
  (vocabulario core, frases prontas + historico, simbolos pessoais + draft +
  audio com openAudioRecorderFor/clearSymbolAudio/removePersonalSymbol,
  categorias, rotina, cenas). pickerBusy preservado nos botoes Camera/Galeria
  (disabled + opacity).
</success_criteria>

<output>
Apos completar, criar `.planning/phases/24-configuracoes-agrupadas-com-drill-down/24-03-SUMMARY.md`.
</output>
</content>
</invoke>