---
phase: 24-configuracoes-agrupadas-com-drill-down
plan: 02
type: execute
wave: 2
depends_on: [24-01]
files_modified:
  - src/App.tsx
autonomous: true
requirements: [CFG-01, CFG-02]
tags: [config, drill-down, app-group, voz, acessibilidade, aparencia, salvia-creme]

must_haves:
  truths:
    - "A sub-tela 'Voz' mostra velocidade, tom e um botao 'Testar voz', com cada controle em sua secao bem espacada — nao mais uma lista densa."
    - "A sub-tela 'Acessibilidade' mostra o seletor de 3 temas (Salvia / Terracota / Sereno Escuro) e o toggle de feedback visual."
    - "A sub-tela 'Aparencia' mostra Escala da interface e Imagens por linha — separadas do bloco anterior 'Perfil'."
    - "Todos os textos das sub-telas do grupo App consomem `theme.typography.*` (Nunito) e cores `theme.colors.*` — sem azul iOS, sem cor hardcoded fora dos tokens."
  artifacts:
    - path: "src/App.tsx"
      provides: "Sub-telas Voz / Acessibilidade / Aparencia restilizadas dentro do drill-down do sheet de Ajustes, mantendo OptionChip / Switch existentes."
      contains: "configRoute === 'voz'"
    - path: "src/App.tsx"
      provides: "Estilos de secao do drill-down (cabecalho de secao, hint, grupo de chips) tokenizados em theme."
      contains: "drillSectionTitle"
  key_links:
    - from: "Sub-tela 'voz'"
      to: "estado pitch / rate / Speech.speak"
      via: "OptionChip onPress + botao 'Testar voz' (handlers existentes)"
      pattern: "Speech.speak"
    - from: "Sub-tela 'acessibilidade'"
      to: "themeName / setThemeName / visualFeedbackEnabled"
      via: "OptionChip de tema + Switch nativo (handlers existentes)"
      pattern: "setThemeName"
    - from: "Sub-tela 'aparencia'"
      to: "uiScale / setUiScale / gridColumns / setGridColumns"
      via: "OptionChip existente + GRID_COLUMNS_OPTIONS"
      pattern: "setGridColumns"
---

<objective>
Restilizar e expandir as 3 sub-telas do grupo "App" (Voz, Acessibilidade,
Aparencia) dentro do drill-down criado no plano 24-01. Em vez do JSX legado
verbatim, cada sub-tela passa a usar o padrao "uma coisa por tela" do sketch 004
variante A: titulo grande no detail header (ja existe), cabecalho de secao
mudo em UPPERCASE para cada grupo de controles, espaco respiravel entre
controles, todos os textos consumindo tokens Nunito.

Mantem-se 100% da logica funcional: handlers `setRate`, `setPitch`,
`Speech.speak`, `setThemeName`, `setVisualFeedbackEnabled`, `setUiScale`,
`setGridColumns` continuam verbatim — apenas o invólucro visual muda.

Purpose: avancar CFG-01 (home agrupada visivel) e CFG-02 (drill-down sem listas
densas) para o grupo App. Tambem promove a separacao logica entre "Aparencia"
(densidade visual) e "Acessibilidade" (tema + feedback) — antes amontoadas em
"Perfil" + "Acessibilidade".
Output: 3 sub-telas restilizadas e funcionais; sub-telas do grupo "Conteudo da
crianca" (plano 24-03) e "Cuidador" (plano 24-04) seguem usando JSX legado
ate seus respectivos planos.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/24-configuracoes-agrupadas-com-drill-down/24-CONTEXT.md
@.planning/phases/24-configuracoes-agrupadas-com-drill-down/24-01-PLAN.md
@.claude/skills/sketch-findings-fala/references/configuracoes.md
@.claude/skills/sketch-findings-fala/references/fundamentos-visuais.md
@src/theme.ts
@src/App.tsx

<interfaces>
<!-- Contratos para restyling do grupo App. Use VERBATIM. -->

Handlers e estado (em src/App.tsx, NAO alterar — apenas consumir):
```ts
const [rate, setRate] = useState<number>(1);                  // ~ TTS
const [pitch, setPitch] = useState<number>(1);
const [themeName, setThemeName] = useState<ThemeName>('default');
const [visualFeedbackEnabled, setVisualFeedbackEnabled] = useState(true);
const [uiScale, setUiScale] = useState<'compacto' | 'padrao' | 'confortavel'>('padrao');
const [gridColumns, setGridColumns] = useState<GridColumns>(...);
const GRID_COLUMNS_OPTIONS: readonly GridColumns[];
Speech.speak(text, { language: 'pt-BR', rate, pitch });
```

Componentes existentes reusados (NAO redesenhar):
- `OptionChip` (linha ~3844) — chip neutro / ativo com cor de tema.
- `Switch` nativo do React Native — toggle binario.

Tokens novos para esta passagem (adicionar em `makeStyles(theme)`):
- `drillSectionTitle` — pequeno UPPERCASE muted, equivalente ao `.sgroup-title`.
- `drillSectionCard` — card surface com border + radius lg + shadow sm.
- `drillSectionBody` — padding interno com gap.
- `drillFieldLabel` — label de controle (acima de chips/switch).
- `drillFieldHint` — hint pt-BR em footnote muted.
- `drillChipRow` — flex row wrap com gap consistente.
- `drillToggleRow` — flex row align center, espaco entre label e Switch.
- `drillPrimaryButton` — botao salvia preenchido (substitui `modalButtonPrimary` legado nesta secao).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Adicionar tokens de estilo do drill-down em makeStyles</name>
  <files>src/App.tsx</files>
  <read_first>
    - src/App.tsx linhas 4368-fim de makeStyles (entender o padrao de adicao)
    - src/App.tsx linhas 5946-6080 (modalSectionTitle, modalHint, modalInput, modalButtonPrimary, settingLabel, settingActions, iosToggleRow — usar como inspiracao mas NAO copiar valores legados; usar tokens de tema)
    - src/theme.ts BLOCO 2 (theme.colors, theme.spacing, theme.radii, theme.typography, theme.shadows)
    - .claude/skills/sketch-findings-fala/references/configuracoes.md (CSS Patterns)
  </read_first>
  <action>
    Adicionar dentro de `StyleSheet.create({...})` em `makeStyles(theme)` os seguintes
    style keys NOVOS (ANTES das chaves legadas; nao mexer nas legadas — serao
    progressivamente substituidas):
    ```ts
    drillSectionTitle: {
      ...theme.typography.caption1,
      color: theme.colors.textMuted,
      textTransform: 'uppercase',
      letterSpacing: 0.6,
      paddingHorizontal: theme.spacing.sm,
      paddingBottom: 6,
      paddingTop: theme.spacing.sm
    },
    drillSectionCard: {
      backgroundColor: theme.colors.surface,
      borderRadius: theme.radii.lg,
      borderWidth: 1,
      borderColor: theme.colors.border,
      padding: theme.spacing.lg,
      gap: theme.spacing.md,
      ...theme.shadows.sm
    },
    drillFieldLabel: {
      ...theme.typography.subheadline,
      color: theme.colors.text
    },
    drillFieldHint: {
      ...theme.typography.footnote,
      color: theme.colors.textMuted
    },
    drillChipRow: {
      flexDirection: 'row',
      flexWrap: 'wrap',
      gap: theme.spacing.sm
    },
    drillToggleRow: {
      flexDirection: 'row',
      alignItems: 'center',
      justifyContent: 'space-between',
      gap: theme.spacing.md,
      paddingVertical: theme.spacing.xs
    },
    drillPrimaryButton: {
      backgroundColor: theme.colors.primary,
      paddingVertical: theme.spacing.md,
      paddingHorizontal: theme.spacing.xl,
      borderRadius: theme.radii.full,
      alignItems: 'center'
    },
    drillPrimaryButtonText: {
      ...theme.typography.headline,
      color: '#FFFFFF'
    },
    drillSecondaryButton: {
      backgroundColor: theme.colors.bgSoft,
      paddingVertical: theme.spacing.md,
      paddingHorizontal: theme.spacing.xl,
      borderRadius: theme.radii.full,
      alignItems: 'center',
      borderWidth: 1,
      borderColor: theme.colors.border
    },
    drillSecondaryButtonText: {
      ...theme.typography.headline,
      color: theme.colors.text
    },
    drillDangerButton: {
      backgroundColor: theme.colors.dangerSoft,
      paddingVertical: theme.spacing.md,
      paddingHorizontal: theme.spacing.xl,
      borderRadius: theme.radii.full,
      alignItems: 'center'
    },
    drillDangerButtonText: {
      ...theme.typography.headline,
      color: theme.colors.danger
    }
    ```
    Nao remover nenhuma chave legada nesta task. Lint deve passar.
  </action>
  <verify>
    <automated>cd "C:/Users/eduau/OneDrive/Área de Trabalho/fala" && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "drillSectionTitle:\|drillSectionCard:\|drillFieldLabel:\|drillFieldHint:\|drillChipRow:\|drillToggleRow:\|drillPrimaryButton:\|drillSecondaryButton:\|drillDangerButton:" src/App.tsx` retorna 9 linhas.
    - `npm run lint` passa.
  </acceptance_criteria>
  <done>9 tokens de estilo do drill-down adicionados, tokenizados via theme.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Restilizar sub-tela 'Voz' usando os novos tokens</name>
  <files>src/App.tsx</files>
  <read_first>
    - src/App.tsx bloco `configRoute === 'voz'` resultante do plano 24-01 (corresponde ao antigo `configSection === 'voz'`, linhas ~3601-3629 pre-24-01)
    - src/App.tsx OptionChip (linha ~3844)
  </read_first>
  <action>
    Substituir o JSX do bloco `configRoute === 'voz' && (...)` por:
    ```tsx
    {configRoute === 'voz' && (
      <>
        <Text style={styles.drillSectionTitle}>VELOCIDADE</Text>
        <View style={styles.drillSectionCard}>
          <Text style={styles.drillFieldLabel}>Velocidade da fala: {rate.toFixed(1)}</Text>
          <View style={styles.drillChipRow}>
            <OptionChip label="0.8" active={rate === 0.8} onPress={() => setRate(0.8)} />
            <OptionChip label="1.0" active={rate === 1}   onPress={() => setRate(1)} />
            <OptionChip label="1.2" active={rate === 1.2} onPress={() => setRate(1.2)} />
          </View>
        </View>

        <Text style={styles.drillSectionTitle}>TOM</Text>
        <View style={styles.drillSectionCard}>
          <Text style={styles.drillFieldLabel}>Tom da voz: {pitch.toFixed(1)}</Text>
          <View style={styles.drillChipRow}>
            <OptionChip label="0.8" active={pitch === 0.8} onPress={() => setPitch(0.8)} />
            <OptionChip label="1.0" active={pitch === 1}   onPress={() => setPitch(1)} />
            <OptionChip label="1.2" active={pitch === 1.2} onPress={() => setPitch(1.2)} />
          </View>
        </View>

        <Pressable
          style={styles.drillPrimaryButton}
          onPress={() =>
            Speech.speak('Olá, esta é a voz do aplicativo.', { language: 'pt-BR', rate, pitch })
          }
          accessibilityRole="button"
          accessibilityLabel="Testar voz"
        >
          <Text style={styles.drillPrimaryButtonText}>Testar voz</Text>
        </Pressable>
      </>
    )}
    ```
    NAO passar `highContrast={isHighContrast}` para OptionChip — a paleta nova
    tem coerencia interna via tokens; o tema "Sereno Escuro" cobre o caso noturno
    automaticamente. (Se restar alguma referencia a `isHighContrast` neste bloco,
    remove-la.)
  </action>
  <verify>
    <automated>cd "C:/Users/eduau/OneDrive/Área de Trabalho/fala" && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "configRoute === 'voz'" src/App.tsx` retorna 1.
    - Dentro do bloco voz, `grep "drillSectionTitle\|drillSectionCard\|drillPrimaryButton"` casa >= 3.
    - Bloco voz NAO contem `styles.modalSectionTitle`, `styles.modalHint`,
      `styles.settingActions`, `styles.modalButtonPrimary` (anti-regressao).
    - Bloco voz NAO contem `isHighContrast`.
    - `npm run lint` passa.
  </acceptance_criteria>
  <done>Sub-tela 'voz' restilizada com 2 cards (Velocidade, Tom) + botao Testar voz dominante, funcionando.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Restilizar sub-telas 'Acessibilidade' e 'Aparencia' usando os novos tokens</name>
  <files>src/App.tsx</files>
  <read_first>
    - src/App.tsx bloco `configRoute === 'acessibilidade'` (do plano 24-01)
    - src/App.tsx bloco `configRoute === 'aparencia'` (do plano 24-01 — era o antigo 'perfil')
    - src/App.tsx OptionChip e definicao de `GRID_COLUMNS_OPTIONS`
    - src/theme.ts (themeName possiveis: 'default' | 'terracota' | 'sereno-escuro')
  </read_first>
  <action>
    1. Substituir o JSX de `configRoute === 'acessibilidade'` por:
       ```tsx
       {configRoute === 'acessibilidade' && (
         <>
           <Text style={styles.drillSectionTitle}>TEMA DO APLICATIVO</Text>
           <View style={styles.drillSectionCard}>
             <Text style={styles.drillFieldHint}>
               O tema muda as cores do aplicativo. Sereno Escuro é um modo escuro calmo para uso noturno ou crianças sensíveis a brilho.
             </Text>
             <View style={styles.drillChipRow}>
               <OptionChip label="Sálvia & Creme" active={themeName === 'default'}        onPress={() => setThemeName('default')} />
               <OptionChip label="Terracota"      active={themeName === 'terracota'}      onPress={() => setThemeName('terracota')} />
               <OptionChip label="Sereno Escuro"  active={themeName === 'sereno-escuro'}  onPress={() => setThemeName('sereno-escuro')} />
             </View>
           </View>

           <Text style={styles.drillSectionTitle}>FEEDBACK</Text>
           <View style={styles.drillSectionCard}>
             <View style={styles.drillToggleRow}>
               <Text style={styles.drillFieldLabel}>Feedback visual ao tocar</Text>
               <Switch
                 value={visualFeedbackEnabled}
                 onValueChange={setVisualFeedbackEnabled}
                 trackColor={{ false: theme.colors.border, true: theme.colors.primary }}
                 thumbColor="#FFFFFF"
                 accessibilityLabel="Alternar feedback visual"
               />
             </View>
             <Text style={styles.drillFieldHint}>
               Quando ativado, um halo discreto aparece ao redor das figuras tocadas.
             </Text>
           </View>
         </>
       )}
       ```
       Observacao: `theme` ja esta no escopo do componente App (linha ~429 ha
       `const styles = useMemo(() => makeStyles(theme), [theme]);`) — usar `theme`
       diretamente como no exemplo. Se nao estiver, capturar via
       `const theme = moduleTheme;` localmente como o resto do componente faz.

    2. Substituir o JSX de `configRoute === 'aparencia'` por:
       ```tsx
       {configRoute === 'aparencia' && (
         <>
           <Text style={styles.drillSectionTitle}>ESCALA DA INTERFACE</Text>
           <View style={styles.drillSectionCard}>
             <Text style={styles.drillFieldHint}>Ajuste a densidade visual conforme a preferência da criança.</Text>
             <View style={styles.drillChipRow}>
               <OptionChip label="Compacto"    active={uiScale === 'compacto'}    onPress={() => setUiScale('compacto')} />
               <OptionChip label="Padrão"      active={uiScale === 'padrao'}      onPress={() => setUiScale('padrao')} />
               <OptionChip label="Confortável" active={uiScale === 'confortavel'} onPress={() => setUiScale('confortavel')} />
             </View>
           </View>

           <Text style={styles.drillSectionTitle}>IMAGENS POR LINHA</Text>
           <View style={styles.drillSectionCard}>
             <Text style={styles.drillFieldHint}>Quantos pictogramas aparecem por linha na grade principal.</Text>
             <View style={styles.drillChipRow}>
               {GRID_COLUMNS_OPTIONS.map(option => (
                 <OptionChip
                   key={`grid-col-${option}`}
                   label={`${option} col`}
                   active={gridColumns === option}
                   onPress={() => setGridColumns(option)}
                 />
               ))}
             </View>
           </View>
         </>
       )}
       ```
    3. Remover qualquer `highContrast={isHighContrast}` desses dois blocos.
       Sereno Escuro cobre o caso noturno automaticamente via tokens de tema.
  </action>
  <verify>
    <automated>cd "C:/Users/eduau/OneDrive/Área de Trabalho/fala" && npm run lint &amp;&amp; npm run test -- --runInBand</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "configRoute === 'acessibilidade'" src/App.tsx` retorna 1.
    - `grep -n "configRoute === 'aparencia'" src/App.tsx` retorna 1.
    - Dentro do bloco acessibilidade: contem `themeName === 'default'`,
      `themeName === 'terracota'` e `themeName === 'sereno-escuro'`.
    - Dentro do bloco aparencia: contem `GRID_COLUMNS_OPTIONS.map`.
    - Ambos os blocos contem `drillSectionTitle` >= 2 vezes cada (um por secao).
    - Ambos os blocos NAO contem `styles.modalSectionTitle`,
      `styles.modalHint`, `styles.settingActions`, `styles.iosToggleRow`,
      `styles.modalButtonPrimary` (anti-regressao).
    - `grep "fontFamily: '" src/App.tsx` retorna 0 (VIS-02 segue fechado).
    - `npm run lint` passa; `npm run test` 22/26 (sem novas falhas).
  </acceptance_criteria>
  <done>Sub-telas 'acessibilidade' e 'aparencia' restilizadas, separadas em secoes nomeadas, com Switch nativo tematizado.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| (sem novas) | Grupo "App" e aberto por design (CFG-03); todas as 3 sub-telas eram acessiveis ao usuario nao-cuidador antes e continuam sendo. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-04 | I (Information Disclosure) | render Voz/Acessibilidade/Aparencia | accept | Sem nova superficie de ataque — visual + IA refatoracao de UI existente; nenhum handler ou persistencia novo. Fluxo de auth (hashSecret, adminPasswordHash, setAdminPassword) preservado verbatim; apenas o invólucro visual e a navegacao mudam. Sem novas chamadas de rede, persistencia ou input handling. |
</threat_model>

<verification>
- `npm run lint` limpo; `npm run test` 22/26 (sem novas falhas).
- `grep "fontFamily: '" src/App.tsx` retorna 0.
- Os 3 blocos do grupo App consomem exclusivamente os tokens novos `drill*` e
  componentes existentes (OptionChip, Switch).
</verification>

<success_criteria>
- CFG-01: home agrupada visivel desde 24-01; sub-telas do grupo App restilizadas reforcam a hierarquia "uma coisa por tela".
- CFG-02: drill-down do grupo App passa do JSX legado para o novo padrao tokenizado com secoes nomeadas em UPPERCASE e cards Salvia & Creme.
- Logica preservada: rate, pitch, themeName, visualFeedbackEnabled, uiScale, gridColumns inalterados.
</success_criteria>

<output>
Apos completar, criar `.planning/phases/24-configuracoes-agrupadas-com-drill-down/24-02-SUMMARY.md`.
</output>
