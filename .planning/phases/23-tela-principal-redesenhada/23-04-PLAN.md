---
phase: 23-tela-principal-redesenhada
plan: 04
type: execute
wave: 4
depends_on: [23-01, 23-02, 23-03]
files_modified:
  - src/App.tsx
autonomous: false
requirements: [TELA-03, TELA-04]
must_haves:
  truths:
    - "No compositor, o botao 'Ouvir' e visivelmente dominante (cerca de 2x a largura de 'Gerar')."
    - "O botao 'Gerar (IA)' aparece rotulado e acionavel, porem menor, como acao de apoio."
    - "Tocar uma figura selecionada no compositor remove apenas aquela figura, em 1 toque."
    - "Um controle discreto 'limpar' aparece somente quando ha figuras selecionadas e apaga todas."
  artifacts:
    - path: "src/App.tsx"
      provides: "Compositor com hierarquia Ouvir-heroi e remocao de figura em 1 toque"
  key_links:
    - from: "src/App.tsx chip da figura selecionada"
      to: "setSelectedSymbols (remocao por indice)"
      via: "onPress (1 toque) em vez de onLongPress"
      pattern: "onPress=\\{\\(\\) => \\{[^}]*filter\\("
---

<objective>
Reestilizar o compositor de frase no layout validado (sketch 003 variante A,
"Ouvir heroi"): "Ouvir" e o botao dominante (~2x a largura), "Gerar (IA)" fica
rotulado e visivel porem menor como acao de apoio. Mudar a remocao de figura
para 1 toque na propria figura (era long-press) e trocar o botao "Deletar" por
um controle discreto "limpar" que so aparece quando ha figuras selecionadas.

Purpose: TELA-03 (hierarquia Ouvir-heroi) e TELA-04 (apagar figura em 1 toque
+ limpar contextual). Honra os anti-padroes do skill: nada de badge vermelho,
nada de caixa de lixeira, Gerar nunca some.
Output: `src/App.tsx` com o compositor reestilizado.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.claude/skills/sketch-findings-fala/references/tela-principal.md
@.claude/skills/sketch-findings-fala/sources/003-compositor-frase/index.html

<interfaces>
<!-- API de tema e estado do compositor -->
theme.colors: accent (terracota — Ouvir), generate (ouro — Gerar IA),
  generateInk, primary, danger, surface, text, textMuted, border ...
theme.radii / theme.shadows / theme.typography — ver 23-01.
Estado/handlers do compositor (src/App.tsx):
  selectedSymbols: SymbolItem[]; setSelectedSymbols(updater).
  clearSymbols() — limpa tudo. handleGenerate() — IA. handlePlay() — TTS.
  isGenerating / isPlaying — flags. saveCustomSymbol() — botao admin "Salvar".
  setNormalizedPhrase('') — reseta a frase normalizada.
JSX do compositor: src/App.tsx ~2416-2484 (composerCard, selectedList, phraseText,
  iconActionRow, clearButton, generateButton, saveGroupButton, playButton).
Memoria do projeto: o botao "Gerar (IA)" e funcionalidade core — deve permanecer
  SEMPRE visivel e rotulado na visao padrao (nunca esconder).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Remocao de figura em 1 toque + controle "limpar" contextual</name>
  <read_first>
    - src/App.tsx linhas 2418-2445 (ScrollView selectedList — os chips das figuras; hoje removem via onLongPress)
    - src/App.tsx linhas 2457-2482 (iconActionRow — clearButton "Deletar", generateButton, saveGroupButton, playButton)
    - src/App.tsx linhas 4798-4837 (estilos selectedList, emptyChipHint, selectedImage, selectedTextChip*)
    - src/App.tsx linhas 5684-5710 (estilos iconActionRow, iconGlyph, clearButton, clearButtonLabel)
    - .claude/skills/sketch-findings-fala/references/tela-principal.md (secao "Apagar figura" + "What to Avoid: Badge vermelho / Caixa de lixeira")
  </read_first>
  <action>
    Em `src/App.tsx`:

    1. Nos chips das figuras selecionadas (~2422-2443): trocar o `onLongPress` por
       `onPress`. O handler permanece igual: remove a figura pelo indice
       (`setSelectedSymbols(prev => prev.filter((_, i) => i !== index))`) e
       `setNormalizedPhrase('')`. Resultado: 1 toque na figura remove ela.
       Manter o `key` e o conteudo do chip (imagem ou selectedTextChip).
    2. Remover o `<Pressable style={styles.clearButton}>` "Deletar" (com glyph 🗑)
       do `iconActionRow` (~2458-2461). Em seu lugar NAO ha botao na linha de acoes.
    3. Adicionar um controle discreto "limpar" que aparece SOMENTE quando
       `selectedSymbols.length > 0`. Posiciona-lo na faixa da frase: dentro do
       `<ScrollView ... contentContainerStyle={styles.selectedList}>` NAO (scroll
       horizontal); em vez disso, colocar uma pequena `<Pressable>` ao lado/acima
       da faixa de chips — concretamente: envolver o ScrollView de chips num
       `<View>` com `flexDirection: 'row'` e, quando `selectedSymbols.length > 0`,
       renderizar apos o ScrollView um `<Pressable style={styles.clearLink}
       onPress={clearSymbols}><Text style={styles.clearLinkText}>limpar</Text></Pressable>`.
    4. Estilos no `makeStyles`:
       - NOVO `clearLink`: `paddingHorizontal: theme.spacing.sm,
         paddingVertical: theme.spacing.xs, alignSelf: 'center'`.
       - NOVO `clearLinkText`: espalhar `...theme.typography.footnote` (Nunito),
         `color: theme.colors.textMuted`, `textDecorationLine: 'underline'`.
       - O estilo `clearButton` / `clearButtonLabel` fica orfao — remover do objeto
         de estilos apos confirmar 0 referencias no JSX.
    5. NAO usar badge ✕ nos chips e NAO criar caixa/botao de lixeira — anti-padroes.
       O comportamento de `clearSymbols` (apagar tudo) ja existe; apenas o gatilho
       muda para o link "limpar" contextual.
  </action>
  <acceptance_criteria>
    - `grep "onLongPress" src/App.tsx` NAO retorna a linha do chip do compositor
      (verificar: a remocao da figura agora e via onPress). Os onLongPress de
      SymbolCard/RoutineStepCard podem permanecer — sao outros componentes.
    - `grep "clearLink" src/App.tsx` retorna >= 2 linhas (estilo + JSX).
    - `grep ">limpar<" src/App.tsx` retorna 1 linha.
    - `grep "styles.clearButton" src/App.tsx` retorna 0 linhas (botao Deletar removido).
    - `grep "clearSymbols" src/App.tsx` ainda retorna >= 1 linha (limpar tudo funcional).
    - `npm run lint` passa sem erros.
  </acceptance_criteria>
  <verify>
    <automated>npm run lint</automated>
  </verify>
  <done>Tocar a figura remove ela em 1 toque; "limpar" aparece so com figuras e apaga todas; sem badge nem caixa de lixeira; lint limpo.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Compositor com hierarquia "Ouvir heroi" (Ouvir dominante, Gerar menor)</name>
  <read_first>
    - src/App.tsx linhas 2416-2484 (composerCard inteiro — apos as mudancas da Task 1)
    - src/App.tsx linhas 4785-4837 (estilos composerCard, selectedList, selectedImage, selectedTextChip*)
    - src/App.tsx linhas 5659-5765 (estilos phraseText, iconActionRow, iconGlyph, generateButton*, generateButtonLabel, saveGroupButton*, playButton*)
    - .claude/skills/sketch-findings-fala/references/tela-principal.md (secao "Compositor" + CSS Patterns .btn-gen/.btn-speak; "What to Avoid: Gerar e Ouvir do mesmo tamanho")
    - .claude/skills/sketch-findings-fala/sources/003-compositor-frase/index.html (variante A)
  </read_first>
  <action>
    Em `src/App.tsx`, no `makeStyles` e no JSX do compositor:

    1. Hierarquia de largura no `iconActionRow` — trocar os `flex: 1` iguais:
       - `playButton` (Ouvir): `flex: 1.9` (botao dominante, ~2x).
       - `generateButton` (Gerar): `flex: 1` (acao de apoio, menor).
       - `saveGroupButton` (Salvar, admin): `flex: 1` — mantem.
       O botao "Gerar (IA)" PERMANECE sempre visivel e rotulado (e core — nunca
       esconder). Apenas a largura relativa muda.
    2. Cores dos botoes via tokens:
       - `playButton`: `backgroundColor: theme.colors.accent` (terracota — heroi).
       - `playButtonLabel`: `color: '#FFFFFF'`, espalhar `...theme.typography.subheadline`.
       - `generateButton`: `backgroundColor: theme.colors.generate` (ouro).
       - `generateButtonLabel`: `color: theme.colors.generateInk`, espalhar
         `...theme.typography.footnote` (rotulo menor que o de Ouvir).
       - `saveGroupButton`: `backgroundColor: theme.colors.bgSoft`.
       - `saveGroupButtonLabel`: `color: theme.colors.text`, espalhar
         `...theme.typography.footnote`.
       - Todos os 3 botoes: `borderRadius: theme.radii.md`.
    3. Card e faixa de frase:
       - `composerCard`: `backgroundColor: theme.colors.surface`,
         `borderRadius: theme.radii.lg`, `borderWidth: 1`,
         `borderColor: theme.colors.border`, espalhar `...theme.shadows.sm`.
       - `phraseText`: `backgroundColor: theme.colors.surface2`,
         `borderColor: theme.colors.border`, `borderRadius: theme.radii.sm`,
         `color: theme.colors.text`, espalhar `...theme.typography.callout` (Nunito).
       - `emptyChipHint`: espalhar `...theme.typography.footnote`,
         `color: theme.colors.textMuted`.
       - `selectedTextChip`: `borderColor: theme.colors.primary`,
         `backgroundColor: theme.colors.primarySoft`, `borderRadius: theme.radii.sm`.
       - `selectedTextChipText`: `color: theme.colors.primaryInk`, espalhar
         `...theme.typography.caption1`, manter `letterSpacing`.
       - `selectedImage`: `borderColor: theme.colors.border`,
         `backgroundColor: theme.colors.surface`, `borderRadius: theme.radii.sm`.
    4. O `<TextInput style={styles.phraseText}>` mantem o override inline
       `{ fontSize: 14 * uiScaleFactor }` (escala de UI).
    5. NAO alterar handlers: handleGenerate, handlePlay, saveCustomSymbol, os
       glyphs (✨ Gerar, ▶/🔊 Ouvir, 💾 Salvar) e a flag isGenerating/isPlaying
       permanecem. Apenas largura, cor e tipografia mudam.
  </action>
  <acceptance_criteria>
    - `grep -A8 "playButton:" src/App.tsx | grep "flex: 1.9"` retorna 1 linha.
    - `grep -A8 "playButton:" src/App.tsx | grep "theme.colors.accent"` retorna 1 linha.
    - `grep -A8 "generateButton:" src/App.tsx | grep "theme.colors.generate"` retorna 1 linha.
    - `grep -A6 "generateButton:" src/App.tsx | grep "flex: 1$"` retorna 1 linha (Gerar menor).
    - `grep "Gerar" src/App.tsx` ainda retorna a label do botao (Gerar permanece rotulado).
    - `grep -A3 "composerCard:" src/App.tsx | grep "theme.colors.surface"` retorna 1 linha.
    - Nas definicoes de playButton/generateButton/composerCard/phraseText,
      `grep "#007AFF"` retorna 0 linhas.
    - `npm run lint` passa sem erros.
    - `npm run test` sem novas falhas alem do baseline (4 pre-existentes).
  </acceptance_criteria>
  <verify>
    <automated>npm run lint; npm run test</automated>
  </verify>
  <done>Ouvir dominante em terracota (~2x), Gerar menor em ouro mas rotulado e acionavel, card e chips com tokens de tema; lint limpo e sem novas falhas.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verificacao humana da tela principal redesenhada</name>
  <what-built>
    A tela principal inteira foi redesenhada (planos 23-01 a 23-04): header sem
    slogan, categorias e busca na paleta calma, cards de pictograma com tile de
    cor de categoria, vocabulario core integrado a grade e compositor "Ouvir heroi"
    com remocao de figura em 1 toque.
  </what-built>
  <how-to-verify>
    1. Rodar `npm run start` e abrir o app no emulador/device.
    2. Tela principal: confirmar que NAO ha slogan "Comunicação assistiva" no header.
    3. Confirmar que os textos aparecem na fonte Nunito (arredondada), nao na fonte de sistema.
    4. Tocar categorias: a categoria ativa fica em verde-salvia, as inativas em tom neutro.
    5. Olhar os cards da grade: cada pictograma tem um bloco de cor suave atras
       (a cor varia por categoria); o rotulo e texto simples abaixo, sem etiqueta colorida.
    6. Confirmar que o vocabulario core (QUERO, NAO, SIM...) aparece como a primeira
       linha da grade, nao como uma barra separada.
    7. Selecionar 2-3 figuras: tocar UMA figura na faixa do compositor remove so ela.
    8. Confirmar que o link "limpar" so aparece quando ha figuras e apaga todas.
    9. No compositor: "Ouvir" e claramente o botao maior (terracota); "Gerar"
       aparece menor (ouro) mas continua rotulado e clicavel.
    10. Testar o fluxo completo: buscar, selecionar, gerar, ouvir, salvar — sem regressao.
  </how-to-verify>
  <resume-signal>Digite "aprovado" ou descreva os problemas visuais/funcionais encontrados.</resume-signal>
</task>

</tasks>

<threat_model>
Sem superficie de ataque nova — fase exclusivamente visual; nenhuma mudanca em
auth, rede, persistencia ou entrada de usuario.
</threat_model>

<verification>
- `npm run lint` limpo.
- `npm run test` sem novas falhas alem das 4 pre-existentes.
- `grep "flex: 1.9" src/App.tsx` => 1 (Ouvir dominante).
- Verificacao humana da tela principal aprovada.
</verification>

<success_criteria>
- Compositor com Ouvir dominante e Gerar menor porem rotulado (TELA-03).
- Figura removida em 1 toque; "limpar" contextual apaga todas (TELA-04).
- Sem badge vermelho, sem caixa de lixeira, Gerar nunca some (anti-padroes honrados).
- Fluxo principal (buscar, selecionar, gerar, ouvir, salvar) sem regressao.
</success_criteria>

<output>
After completion, create `.planning/phases/23-tela-principal-redesenhada/23-04-SUMMARY.md`
</output>
