---
phase: 23-tela-principal-redesenhada
plan: 02
type: execute
wave: 2
depends_on: [23-01]
files_modified:
  - src/App.tsx
autonomous: true
requirements: [TELA-01]
must_haves:
  truths:
    - "A barra de categorias usa as pills do tema Salvia & Creme: ativa em salvia, inativa em tom neutro suave."
    - "O campo de busca usa fundo suave do tema, cantos generosos e a fonte Nunito."
    - "A area de lista (listCard) usa o fundo agrupado do tema, nao o cinza iOS."
  artifacts:
    - path: "src/App.tsx"
      provides: "Barra de categorias + busca + listCard reestilizados com tokens de tema"
  key_links:
    - from: "src/App.tsx categoryButton* / searchInput / searchButton styles"
      to: "theme.colors / theme.typography"
      via: "makeStyles(theme)"
      pattern: "theme\\.colors\\.primary|theme\\.typography\\."
---

<objective>
Reestilizar a fileira de categorias, o campo de busca e o container da lista
(`listCard`) da tela principal com os tokens do tema "Salvia & Creme". As pills
de categoria passam a usar salvia para o estado ativo e tom neutro suave para o
inativo; o campo de busca usa fundo suave e cantos generosos. Todo texto adota
`theme.typography.*` (fonte Nunito).

Purpose: Continua o layout enxuto (TELA-01) trocando a estetica iOS azul da
barra de categorias e busca pela paleta calma; segue fechando o gap VIS-02.
Output: `src/App.tsx` com categorias, busca e listCard reestilizados.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.claude/skills/sketch-findings-fala/references/tela-principal.md
@.claude/skills/sketch-findings-fala/references/fundamentos-visuais.md

<interfaces>
<!-- API de tema (src/theme.ts) — usar diretamente -->
theme.colors: bg, bgSoft, surface, surface2, border, borderStrong, text,
  textMuted, textSoft, primary, primaryHover, primarySoft, primaryInk, ...
theme.radii: sm=10, md=16, lg=22, xl=30, full=9999.
theme.typography.* — { fontSize, lineHeight, fontFamily(Nunito) }.
makeStyles(theme: Theme); styles = useMemo(() => makeStyles(theme), [theme]).
CategoryButton (src/App.tsx ~3788) e funcao auxiliar: le `moduleStyles`; aplica
  styles.categoryButton / categoryButtonActive / categoryButtonText / categoryButtonTextActive
  e categoryButtonHighContrast. Reestilizar via os objetos de estilo, sem refatorar o componente.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Reestilizar a barra de categorias (pills) com tokens de tema</name>
  <read_first>
    - src/App.tsx linhas 3788-3807 (componente CategoryButton — confirmar quais estilos consome)
    - src/App.tsx linhas 4621-4656 (estilos: categoriesScroll, categoriesRow, categoryButton, categoryButtonHighContrast, categoryButtonActive, categoryButtonText, categoryButtonTextActive)
    - src/theme.ts linhas 366-378 (theme.typography) e 434-467 (theme.default.colors)
    - .claude/skills/sketch-findings-fala/references/tela-principal.md (secao Layout — fileira de categorias)
  </read_first>
  <action>
    Em `src/App.tsx`, reescrever no objeto `makeStyles`:
    - `categoryButton`: `backgroundColor: theme.colors.bgSoft`,
      `borderRadius: theme.radii.full` (pill). Manter paddings e minHeight.
    - `categoryButtonActive`: `backgroundColor: theme.colors.primary` (salvia).
    - `categoryButtonText`: espalhar `...theme.typography.subheadline` (Nunito),
      `color: theme.colors.text`. Remover o `fontWeight` literal.
    - `categoryButtonTextActive`: `color: '#FFFFFF'` (texto sobre salvia — mantem
      contraste; salvia e cor escura o suficiente). Remover `fontWeight` literal.
    - `categoryButtonHighContrast`: `backgroundColor: theme.colors.surface2`
      (no tema escuro `bgSoft` ja e escuro; surface2 da um leve destaque).
    NAO alterar `categoriesScroll`/`categoriesRow` (apenas layout, sem cor) — opcional
    ajustar `gap` para `theme.spacing.sm`. NAO mudar a logica de `handleCategoryClick`
    nem a ordem dos `CategoryButton` no JSX.
  </action>
  <acceptance_criteria>
    - `grep -A6 "categoryButtonActive:" src/App.tsx | grep "theme.colors.primary"` retorna 1 linha.
    - `grep -A4 "categoryButton:" src/App.tsx | grep "theme.colors.bgSoft"` retorna 1 linha.
    - `grep -A4 "categoryButtonText:" src/App.tsx | grep "theme.typography.subheadline"` retorna 1 linha.
    - Nas definicoes de categoryButton*, `grep "#007AFF"` retorna 0 linhas.
    - `npm run lint` passa sem erros.
  </acceptance_criteria>
  <verify>
    <automated>npm run lint</automated>
  </verify>
  <done>Pills de categoria em salvia (ativo) / tom neutro (inativo) via tokens; texto em Nunito; lint limpo.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Reestilizar o campo de busca e o container da lista (listCard)</name>
  <read_first>
    - src/App.tsx linhas 2157-2176 (JSX da searchRow: searchInput, searchButton, searchClearButton)
    - src/App.tsx linhas 4568-4613 (estilos: searchRow, searchInput, inputHighContrast, searchButton, searchClearButton, searchClearButtonText, searchButtonText)
    - src/App.tsx linhas 4657-4673 (estilos: listCard, grid, loadingState)
    - src/App.tsx linha 2181 (ActivityIndicator color hardcoded "#5B8C7A")
    - src/theme.ts linhas 366-378 e 434-467
  </read_first>
  <action>
    Em `src/App.tsx`, reescrever no `makeStyles`:
    - `searchInput`: `backgroundColor: theme.colors.surface2`,
      `borderRadius: theme.radii.sm`, `color: theme.colors.text`, e espalhar
      `...theme.typography.callout` (Nunito) mantendo `flex: 1`, `height`, paddings.
    - `searchButton`: `backgroundColor: theme.colors.primary` (era #007AFF),
      `borderRadius: theme.radii.sm`.
    - `searchButtonText`: `color: '#FFFFFF'`, espalhar `...theme.typography.subheadline`.
    - `searchClearButton`: `backgroundColor: theme.colors.bgSoft`,
      `borderRadius: theme.radii.full`.
    - `searchClearButtonText`: `color: theme.colors.textMuted`.
    - `listCard`: `backgroundColor: theme.colors.bgSoft`,
      `borderRadius: theme.radii.lg`. Manter `flex: 1`, `overflow: 'hidden'`.
    JSX: na linha do `<ActivityIndicator size="large" color="#5B8C7A" />` (~2181),
    trocar para `color={theme.colors.primary}` — `theme` esta no escopo de App.
    NAO mudar a logica de `handleSearch`/`clearSearchAndClose` nem a condicao
    `isSearchOpen`. `inputHighContrast` permanece como esta (alto contraste e
    coberto pelo tema sereno-escuro; este array legado ainda existe no JSX).
  </action>
  <acceptance_criteria>
    - `grep -A6 "searchButton:" src/App.tsx | grep "theme.colors.primary"` retorna 1 linha.
    - `grep -A5 "searchInput:" src/App.tsx | grep "theme.colors.surface2"` retorna 1 linha.
    - `grep -A4 "listCard:" src/App.tsx | grep "theme.colors.bgSoft"` retorna 1 linha.
    - `grep "color=\"#5B8C7A\"" src/App.tsx` retorna 0 linhas (ActivityIndicator tokenizado).
    - Nas definicoes de searchInput/searchButton/listCard, `grep "#007AFF"` e
      `grep "#F2F2F7"` retornam 0 linhas.
    - `npm run lint` passa sem erros.
    - `npm run test` sem novas falhas alem do baseline (4 pre-existentes).
  </acceptance_criteria>
  <verify>
    <automated>npm run lint; npm run test</automated>
  </verify>
  <done>Busca e listCard com tokens de tema, texto em Nunito, sem azul iOS; lint limpo e sem novas falhas.</done>
</task>

</tasks>

<threat_model>
Sem superficie de ataque nova — fase exclusivamente visual; nenhuma mudanca em
auth, rede, persistencia ou entrada de usuario.
</threat_model>

<verification>
- `npm run lint` limpo.
- `npm run test` sem novas falhas alem das 4 pre-existentes.
- Categorias, busca e listCard sem `#007AFF` nas suas definicoes de estilo.
- `grep "theme.typography" src/App.tsx` cresceu em relacao ao plano 23-01 (mais textos com Nunito).
</verification>

<success_criteria>
- Barra de categorias com pills salvia/neutro (TELA-01 — layout enxuto, parte 2).
- Campo de busca e container da lista usam a paleta calma.
- Textos de categoria e busca em fonte Nunito (VIS-02 avancando).
- Sem regressao funcional em categorias e busca.
</success_criteria>

<output>
After completion, create `.planning/phases/23-tela-principal-redesenhada/23-02-SUMMARY.md`
</output>
