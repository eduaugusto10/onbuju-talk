---
phase: 23-tela-principal-redesenhada
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/categoryColors.ts
  - src/App.tsx
autonomous: true
requirements: [TELA-01]
must_haves:
  truths:
    - "A tela principal abre sem o slogan 'Comunicação assistiva' no header."
    - "O header (engrenagem, titulo, busca, badge admin) usa os tokens do tema Salvia & Creme e a fonte Nunito."
    - "Existe uma funcao de mapeamento categoria -> familia de cor pronta para o card consumir na fase 23-03."
  artifacts:
    - path: "src/categoryColors.ts"
      provides: "Mapeamento categoria ARASAAC -> 5 familias de cor suave + tom neutro"
      contains: "export function categoryColorFamily"
    - path: "src/App.tsx"
      provides: "Header reestilizado sem slogan, com tokens de tema"
  key_links:
    - from: "src/App.tsx headerCard styles"
      to: "theme.colors / theme.typography"
      via: "makeStyles(theme)"
      pattern: "theme\\.colors\\.|theme\\.typography\\."
---

<objective>
Remover o slogan do header e reestilizar o header da tela principal (engrenagem,
titulo "ONBUJU TALK", botao de busca, badge ADMIN) com os tokens do tema
"Salvia & Creme" da fase 22, aplicando os tokens `theme.typography.*` (que
carregam a fonte Nunito) a todo texto do header. Tambem cria o modulo
`src/categoryColors.ts` com a funcao de mapeamento categoria -> familia de cor,
consumida pelo card na fase 23-03.

Purpose: Inicia o layout enxuto (TELA-01) cortando decoracao do header e
estabelece a fundacao de cor de categoria; comeca a fechar o gap VIS-02.
Output: `src/categoryColors.ts` novo; header de `src/App.tsx` reestilizado.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.claude/skills/sketch-findings-fala/references/tela-principal.md
@.claude/skills/sketch-findings-fala/references/fundamentos-visuais.md

<interfaces>
<!-- API de tema (src/theme.ts) — usar diretamente, nao explorar -->

theme.colors: bg, bgSoft, surface, surface2, border, borderStrong, text,
  textMuted, textSoft, primary, primaryHover, primarySoft, primaryInk,
  generate, generateHover, generateSoft, generateInk, accent, accentHover,
  accentSoft, danger, dangerSoft, success, star.
theme.radii: sm=10, md=16, lg=22, xl=30, full=9999.
theme.spacing: xs=4, sm=8, md=12, lg=16, xl=20, xxl=24, xxxl=32.
theme.shadows: sm, md, lg (objetos ViewStyle de sombra).
theme.typography: largeTitle, title1, title2, title3, headline, body, callout,
  subheadline, footnote, caption1, caption2 — cada um e
  { fontSize, lineHeight, fontFamily } e o fontFamily ja e Nunito.

makeStyles(theme: Theme) — factory de StyleSheet no fim de src/App.tsx;
  `styles` em App vem de useMemo(() => makeStyles(theme), [theme]).
isHighContrast = isDarkTheme (alias) — manter os arrays `[styles.x, isHighContrast && styles.xHC]`.
As 5 familias de cor de categoria (de tela-principal.md): acoes #DBE6DE,
  comida #F6E6C7, pessoas #F0DCCE, lazer #E3E8D2, rotina #E7E0EC; neutro #ECE6D8.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Criar src/categoryColors.ts com o mapeamento categoria -> familia de cor</name>
  <read_first>
    - .claude/skills/sketch-findings-fala/references/tela-principal.md (secao "Card de pictograma" — as 5 cores pasteis)
    - src/constants.ts (FALLBACK_CATEGORIES — exemplos de nomes de categoria reais)
    - src/theme.ts (linhas 243-282 — tipo ThemeColors, para entender que sao strings hex)
  </read_first>
  <behavior>
    - categoryColorFamily('Ações') retorna a cor da familia 'acoes' (#DBE6DE).
    - categoryColorFamily('Alimentação') retorna a cor da familia 'comida' (#F6E6C7).
    - categoryColorFamily('Pessoas') retorna a cor da familia 'pessoas' (#F0DCCE).
    - categoryColorFamily('Brinquedos') retorna a cor da familia 'lazer' (#E3E8D2).
    - categoryColorFamily('Rotina') retorna a cor da familia 'rotina' (#E7E0EC).
    - categoryColorFamily('CategoriaDesconhecidaXYZ') retorna o tom neutro (#ECE6D8).
    - A funcao normaliza o texto (lowercase, sem acento) antes de casar.
  </behavior>
  <action>
    Criar o arquivo `src/categoryColors.ts`. Definir:

    1. `export const CATEGORY_COLOR_FAMILIES` — objeto Record com 6 chaves e
       valores hex literais: `acoes: '#DBE6DE'`, `comida: '#F6E6C7'`,
       `pessoas: '#F0DCCE'`, `lazer: '#E3E8D2'`, `rotina: '#E7E0EC'`,
       `neutro: '#ECE6D8'`. Tipar como `as const`.
    2. `export type CategoryColorFamily = keyof typeof CATEGORY_COLOR_FAMILIES`.
    3. Uma constante interna `FAMILY_KEYWORDS: Record<Exclude<CategoryColorFamily,'neutro'>, string[]>`
       com listas de termos pt-BR (em minusculo, sem acento) por familia. Sugestao
       de cobertura (escolha do executor pode estender):
       - acoes: ['acoes', 'acao', 'verbos', 'fazer']
       - comida: ['alimentacao', 'comida', 'comer', 'beber', 'bebida', 'lanche', 'fruta']
       - pessoas: ['pessoas', 'familia', 'pessoal', 'gente', 'eu']
       - lazer: ['lazer', 'brincar', 'brinquedo', 'brinquedos', 'jogo', 'jogos', 'escola', 'diversao']
       - rotina: ['rotina', 'higiene', 'saude', 'casa', 'lugares', 'sentimentos', 'dia']
    4. Um helper interno `normalize(s: string): string` que faz
       `s.toLowerCase().normalize('NFD').replace(/[̀-ͯ]/g, '').trim()`.
    5. `export function categoryColorFamily(category: string | undefined | null): string`
       que normaliza a entrada, procura a primeira familia cujo array de keywords
       contenha um termo que seja substring da entrada (ou a entrada substring do termo),
       e retorna `CATEGORY_COLOR_FAMILIES[familia]`; sem correspondencia retorna
       `CATEGORY_COLOR_FAMILIES.neutro`.

    Strings de codigo em ingles; este modulo nao tem strings de UI. TypeScript strict.
  </action>
  <acceptance_criteria>
    - `grep "export function categoryColorFamily" src/categoryColors.ts` retorna 1 linha.
    - `grep "#DBE6DE" src/categoryColors.ts` e `grep "#ECE6D8" src/categoryColors.ts` retornam 1 linha cada.
    - `grep -c "'#" src/categoryColors.ts` retorna >= 6 (as 6 cores das familias).
    - `npm run lint` (tsc --noEmit) passa sem erros.
  </acceptance_criteria>
  <verify>
    <automated>npm run lint</automated>
  </verify>
  <done>src/categoryColors.ts existe, exporta categoryColorFamily e as 6 cores; lint limpo.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Remover o slogan e reestilizar o header com tokens de tema + Nunito</name>
  <read_first>
    - src/App.tsx linhas 2039-2086 (JSX do header: headerCard, headerRow, title, headerTagline, menu, busca, adminBadge)
    - src/App.tsx linhas 4461-4567 (estilos: headerCard, title, headerTagline, menuButton, menuIcon, adminBadge*, searchToggleButton*)
    - src/theme.ts linhas 366-378 (tokens theme.typography.*) e 434-467 (theme.default colors)
    - .claude/skills/sketch-findings-fala/references/tela-principal.md (secao Layout — "header (engrenagem · 'Fala' · busca)" e "What to Avoid: Slogan/textos decorativos")
  </read_first>
  <action>
    Em `src/App.tsx`:

    1. JSX (regiao ~2057-2060): REMOVER o `<Text style={styles.headerTagline}...>`
       que renderiza "Comunicação assistiva". Manter apenas o `<Text style={styles.title}...>`
       dentro de `headerTextBlock`. O slogan e decoracao — anti-padrao do skill.
    2. Estilos em `makeStyles` — reescrever os seguintes objetos usando tokens
       (substituir os valores literais legacy/iOS):
       - `headerCard`: `backgroundColor: theme.colors.surface`,
         `borderBottomColor: theme.colors.border` (manter hairline),
         manter paddings; pode adicionar `borderBottomLeftRadius`/`Right` se desejado — opcional.
       - `title`: espalhar `...theme.typography.title2` (carrega fontFamily Nunito)
         e `color: theme.colors.text`. Remover `fontWeight` literal (o peso vem da Nunito).
       - `menuButton`: `backgroundColor: theme.colors.primarySoft`,
         `borderRadius: theme.radii.full`.
       - `menuIcon`: `color: theme.colors.primaryInk`.
       - `adminBadgeOn`: `backgroundColor: theme.colors.primarySoft`.
       - `adminBadgeText`: `color: theme.colors.primaryInk`, e espalhar
         `...theme.typography.caption1` (Nunito) mantendo `letterSpacing: 0.3`.
       - `searchToggleButton`: `backgroundColor: theme.colors.bgSoft`,
         `borderRadius: theme.radii.sm`.
       - `searchToggleButtonActive`: `backgroundColor: theme.colors.primarySoft`,
         `borderColor: theme.colors.primary`.
    3. O JSX do header em ~2058 aplica `{ fontSize: 17 * uiScaleFactor }` inline ao
       title — MANTER esse override de escala (o token define a base, o inline escala).
    4. Pode-se remover o estilo `headerTagline` do objeto de estilos (orfao apos a
       remocao do JSX) — opcional, mas se removido confirmar 0 referencias restantes.
    5. NAO alterar o comportamento: o botao de engrenagem ainda chama `openConfigModal`,
       o toggle de busca mantem a logica de LayoutAnimation, o badge ADMIN so aparece
       com `isAdmin`. Apenas valores visuais mudam.
  </action>
  <acceptance_criteria>
    - `grep "Comunicação assistiva" src/App.tsx` retorna 0 linhas.
    - `grep "headerTagline" src/App.tsx` retorna 0 linhas (estilo e JSX removidos) OU,
      se mantido o estilo orfao, no maximo 1 linha (a definicao do estilo) e 0 no JSX.
    - `grep "theme.typography.title2" src/App.tsx` retorna >= 1 linha.
    - `grep "fontFamily" src/App.tsx` retorna >= 1 linha (via tokens — fecha parte do VIS-02).
    - No bloco de estilos do header (headerCard/menuButton/menuIcon/adminBadge*),
      `grep "#007AFF"` dentro dessas definicoes retorna 0 (azul iOS removido do header).
    - `npm run lint` passa sem erros.
    - `npm run test` nao introduz novas falhas (baseline: 4 falhas pre-existentes de arasaacService).
  </acceptance_criteria>
  <verify>
    <automated>npm run lint; npm run test</automated>
  </verify>
  <done>Header sem slogan, usando theme.colors/theme.typography; titulo na fonte Nunito; lint limpo e sem novas falhas de teste.</done>
</task>

</tasks>

<threat_model>
Sem superficie de ataque nova — fase exclusivamente visual; nenhuma mudanca em
auth, rede, persistencia ou entrada de usuario. O modulo `src/categoryColors.ts`
e uma funcao pura sem I/O. Nenhum threat aplicavel.
</threat_model>

<verification>
- `npm run lint` (tsc --noEmit) limpo.
- `npm run test` sem novas falhas alem das 4 pre-existentes herdadas.
- `grep "Comunicação assistiva" src/App.tsx` => 0.
- `src/categoryColors.ts` existe e exporta `categoryColorFamily`.
</verification>

<success_criteria>
- Slogan removido do header (TELA-01 — layout enxuto, parte 1).
- Header consome theme.colors e theme.typography (fonte Nunito aplicada — VIS-02 parcial).
- Funcao de mapeamento categoria -> cor pronta para a fase 23-03.
- Sem regressao funcional no header (config, busca, badge).
</success_criteria>

<output>
After completion, create `.planning/phases/23-tela-principal-redesenhada/23-01-SUMMARY.md`
</output>
