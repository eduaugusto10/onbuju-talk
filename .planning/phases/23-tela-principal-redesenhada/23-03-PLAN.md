---
phase: 23-tela-principal-redesenhada
plan: 03
type: execute
wave: 3
depends_on: [23-01, 23-02]
files_modified:
  - src/App.tsx
autonomous: true
requirements: [TELA-01, TELA-02]
must_haves:
  truths:
    - "Cada card de pictograma mostra a cor da categoria no bloco (tile) atras do pictograma."
    - "O rotulo do card e texto simples abaixo do tile, nunca etiqueta colorida."
    - "A estrela de favorito e um overlay circular no canto superior direito do card."
    - "O vocabulario core aparece como a primeira linha fixa da grade, nao como barra separada."
  artifacts:
    - path: "src/App.tsx"
      provides: "SymbolCard com tile de cor de categoria; vocabulario core integrado a grade"
  key_links:
    - from: "src/App.tsx SymbolCard"
      to: "src/categoryColors.ts categoryColorFamily"
      via: "import + chamada com item.category"
      pattern: "categoryColorFamily\\("
    - from: "src/App.tsx grade (FlatList)"
      to: "coreVocabulary"
      via: "ListHeaderComponent renderizando o vocabulario core"
      pattern: "ListHeaderComponent"
---

<objective>
Refazer o `SymbolCard` no estilo validado (sketch 002 variante D): card limpo
em superficie quente, com a cor da categoria preenchendo um "tile" arredondado
ATRAS do pictograma e o rotulo como texto simples abaixo. A estrela de favorito
vira overlay circular no canto. Integrar o vocabulario core como a PRIMEIRA
LINHA FIXA da grade (via `ListHeaderComponent`), removendo a barra de vocabulario
separada que competia visualmente.

Purpose: TELA-02 (card com cor de categoria) e a parte de TELA-01 que remove a
barra de vocabulario separada preservando o vocabulario core integrado.
Output: `src/App.tsx` com SymbolCard refeito e vocabulario core na grade.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.claude/skills/sketch-findings-fala/references/tela-principal.md
@.claude/skills/sketch-findings-fala/sources/002-card-pictograma/index.html

<interfaces>
<!-- API de tema e modulo de cor de categoria -->
theme.colors / theme.radii / theme.shadows / theme.typography — ver 23-01.
src/categoryColors.ts (criado em 23-01):
  export function categoryColorFamily(category: string | undefined | null): string
  — retorna a cor hex da familia (5 pasteis) ou o tom neutro #ECE6D8.

SymbolItem (src/App.tsx): { id, label, imageUrl, category, ... } — `category`
  e a string da categoria ARASAAC (pode ser 'personal' para simbolos pessoais).
SymbolCard (src/App.tsx ~3908): auxiliar, le `moduleStyles`; recebe
  item, columns, favorite, onPress, onFavoritePress, onLongPress, hasAudio, isAdmin.
  isDense = columns>=4; isUltraDense = columns>=5.
coreVocabulary: string[] (estado em App). addCoreWord(word) ja existe.
A grade "normal" e o FlatList em src/App.tsx ~2357-2379 (data=symbolsToRender).
A barra de vocabulario core separada vive em ~2383-2414 (styles.coreVocabBar).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Refazer o SymbolCard com tile de cor de categoria</name>
  <read_first>
    - src/App.tsx linhas 3908-3953 (componente SymbolCard atual)
    - src/App.tsx linhas 4683-4750 (estilos: symbolCard, symbolCardDense, symbolCardUltraDense, favoriteButton*, symbolImage*, symbolLabel*)
    - src/App.tsx linhas 5139-5153 (estilos audioBadge, audioBadgeText)
    - src/categoryColors.ts (a funcao categoryColorFamily — criada em 23-01)
    - .claude/skills/sketch-findings-fala/references/tela-principal.md (secao "Card de pictograma" + CSS Patterns .pcard / .pic / .plabel)
    - .claude/skills/sketch-findings-fala/sources/002-card-pictograma/index.html (referencia visual da variante D)
  </read_first>
  <action>
    Em `src/App.tsx`:

    1. Adicionar no topo o import: `import { categoryColorFamily } from './categoryColors';`
       (junto aos outros imports relativos).
    2. No componente `SymbolCard`, dentro do corpo, calcular:
       `const tileColor = categoryColorFamily(item.category);`
    3. Reestruturar o JSX do card para o padrao "tile atras do pictograma":
       - O `<Pressable style={styles.symbolCard}...>` continua sendo o card externo.
       - Envolver o `<CachedImage>` num `<View>` com `style={[styles.symbolTile, isDense && styles.symbolTileDense, isUltraDense && styles.symbolTileUltraDense, { backgroundColor: tileColor }]}`.
         O `<CachedImage>` fica DENTRO desse View.
       - O `<Text style={styles.symbolLabel}>` permanece ABAIXO do tile, fora dele.
       - O `audioBadge` e o `favoriteButton` permanecem como overlays do card
         (position absolute) — NAO dentro do tile.
    4. Estilos em `makeStyles`:
       - `symbolCard`: `backgroundColor: theme.colors.surface`,
         `borderRadius: theme.radii.md`, `borderWidth: 1`,
         `borderColor: theme.colors.border`, espalhar `...theme.shadows.sm`.
         Manter flex/margin/padding/minHeight/alignItems.
       - NOVO `symbolTile`: `width: 96, height: 96, borderRadius: 18,
         alignItems: 'center', justifyContent: 'center'` (a cor vem inline).
       - NOVO `symbolTileDense`: `width: 60, height: 60, borderRadius: 14`.
       - NOVO `symbolTileUltraDense`: `width: 46, height: 46, borderRadius: 12`.
       - `symbolImage` / `symbolImageDense` / `symbolImageUltraDense`: manter os
         tamanhos atuais (96/60/46) — a imagem preenche o tile.
       - `symbolLabel`: espalhar `...theme.typography.subheadline` (Nunito),
         `color: theme.colors.text`, manter `marginTop`, `width: '100%'`,
         `textAlign: 'center'`. Remover `fontWeight` literal.
       - `symbolLabelDense`: manter `fontSize: 12` (override de densidade).
       - `favoriteButton`: posicionar como overlay — `position: 'absolute',
         top: 6, right: 6, zIndex: 2`, `backgroundColor: theme.colors.surface`,
         `borderRadius: theme.radii.full`, manter sombra (espalhar `...theme.shadows.sm`).
         REMOVER `alignSelf: 'flex-end'`.
       - `favoriteButtonText`: `color: theme.colors.star`.
       - `audioBadge`: `backgroundColor: theme.colors.primary` (manter position/size).
    5. NAO mudar props nem comportamento: onPress/onFavoritePress/onLongPress
       continuam iguais; favorite ainda so aparece com `isAdmin`.
  </action>
  <acceptance_criteria>
    - `grep "import { categoryColorFamily }" src/App.tsx` retorna 1 linha.
    - `grep "categoryColorFamily(item.category)" src/App.tsx` retorna 1 linha.
    - `grep "symbolTile:" src/App.tsx` retorna 1 linha (novo estilo do tile).
    - `grep -A3 "symbolCard:" src/App.tsx | grep "theme.colors.surface"` retorna 1 linha.
    - `grep -A3 "favoriteButton:" src/App.tsx | grep "position: 'absolute'"` retorna 1 linha.
    - `grep -A4 "symbolLabel:" src/App.tsx | grep "theme.typography"` retorna 1 linha.
    - `npm run lint` passa sem erros.
  </acceptance_criteria>
  <verify>
    <automated>npm run lint</automated>
  </verify>
  <done>SymbolCard com tile colorido por categoria atras do pictograma, rotulo em texto abaixo, estrela como overlay circular; lint limpo.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Integrar o vocabulario core como primeira linha fixa da grade</name>
  <read_first>
    - src/App.tsx linhas 2356-2380 (FlatList da grade normal — data=symbolsToRender)
    - src/App.tsx linhas 2383-2414 (barra de vocabulario core separada — coreVocabBar — a remover)
    - src/App.tsx linhas 4839-4874 (estilos coreVocabBar*, coreVocabRow, coreVocabButton*, coreVocabButtonText*)
    - .claude/skills/sketch-findings-fala/references/tela-principal.md (secao Layout: "o core pode ser a primeira linha fixa DA grade, não uma 2ª barra"; "What to Avoid: Barra de vocabulário core separada")
  </read_first>
  <action>
    Em `src/App.tsx`:

    1. REMOVER o bloco JSX da barra de vocabulario core separada (~2383-2414):
       o `<View style={styles.coreVocabBar}...>` inteiro com seu ScrollView e map.
       Essa barra competia como segunda barra — anti-padrao do skill.
    2. No FlatList da grade "normal" (data=`symbolsToRender`, ~2357), adicionar a
       prop `ListHeaderComponent` que renderiza o vocabulario core como a primeira
       linha fixa, SOMENTE quando `activeCategory` nao for scenes/routine e
       `coreVocabulary.length > 0`. O header deve ser um `<View style={styles.coreVocabRow}>`
       horizontal com scroll (`<ScrollView horizontal>`), renderizando cada palavra
       como um `<Pressable style={styles.coreVocabButton} onPress={() => addCoreWord(word)}>`
       com `<Text style={styles.coreVocabButtonText}>{word.toUpperCase()}</Text>`.
       Mantem `addCoreWord`, `accessibilityRole`, `accessibilityLabel` como na barra antiga.
    3. Reestilizar (no `makeStyles`) os estilos do vocabulario core agora integrado:
       - `coreVocabRow`: manter flex-direction row + gap; adicionar
         `paddingHorizontal: theme.spacing.sm`, `paddingBottom: theme.spacing.sm`.
       - `coreVocabButton`: `backgroundColor: theme.colors.primarySoft`,
         `borderRadius: theme.radii.full`. Manter paddings/minWidth.
       - `coreVocabButtonText`: `color: theme.colors.primaryInk`, espalhar
         `...theme.typography.caption1` (Nunito), manter `letterSpacing`.
    4. Os estilos `coreVocabBar`, `coreVocabBarHighContrast`, `coreVocabButtonHighContrast`,
       `coreVocabButtonTextHighContrast` ficam orfaos — podem ser removidos do objeto
       de estilos (confirmar 0 referencias no JSX antes de remover). O JSX inline
       `{ fontSize: 14 * uiScaleFactor }` do texto antigo pode ser mantido como override
       de escala no novo header.
    5. NAO mexer no editor de vocabulario core da config (configSection 'vocabulario',
       ~2690-2783) — e escopo da fase 24 e usa estilos `coreVocabEditor*` diferentes.
    6. NAO alterar o comportamento: a grade ainda renderiza `symbolsToRender`, o
       vocabulario core ainda chama `addCoreWord`. Apenas a posicao/estilo mudam.
  </action>
  <acceptance_criteria>
    - `grep "styles.coreVocabBar" src/App.tsx` retorna 0 linhas (barra separada removida).
    - `grep "ListHeaderComponent" src/App.tsx` retorna >= 1 linha.
    - `grep "addCoreWord(word)" src/App.tsx` retorna 1 linha (vocabulario core ainda funcional).
    - `grep -A4 "coreVocabButton:" src/App.tsx | grep "theme.colors.primarySoft"` retorna 1 linha.
    - `grep -A4 "coreVocabButtonText:" src/App.tsx | grep "theme.typography.caption1"` retorna 1 linha.
    - `npm run lint` passa sem erros.
    - `npm run test` sem novas falhas alem do baseline (4 pre-existentes).
  </acceptance_criteria>
  <verify>
    <automated>npm run lint; npm run test</automated>
  </verify>
  <done>Vocabulario core e a primeira linha fixa da grade via ListHeaderComponent; barra separada removida; lint limpo e sem novas falhas.</done>
</task>

</tasks>

<threat_model>
Sem superficie de ataque nova — fase exclusivamente visual; nenhuma mudanca em
auth, rede, persistencia ou entrada de usuario.
</threat_model>

<verification>
- `npm run lint` limpo.
- `npm run test` sem novas falhas alem das 4 pre-existentes.
- `grep "styles.coreVocabBar" src/App.tsx` => 0 (sem barra separada).
- SymbolCard usa `categoryColorFamily` para a cor do tile.
</verification>

<success_criteria>
- Card com cor de categoria no tile atras do pictograma, rotulo em texto (TELA-02).
- Vocabulario core integrado como primeira linha fixa da grade, sem barra separada (TELA-01).
- Estrela de favorito como overlay circular no canto.
- Sem regressao: selecionar simbolo e adicionar palavra-core continuam funcionando.
</success_criteria>

<output>
After completion, create `.planning/phases/23-tela-principal-redesenhada/23-03-SUMMARY.md`
</output>
